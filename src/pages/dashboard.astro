---
import Layout from '../layouts/Layout.astro';
import Toast from '../components/Toast.astro';
import ModalAlert from '../components/ModalAlert.astro';
import NavbarGuru from '../components/NavbarGuru.astro';

---

<Layout title="Dashboard Guru - yukNgaji">
    <div class="px-6 py-8 pb-16 min-h-screen flex flex-col">

        <header class="flex justify-between items-center mb-8 relative">
            <div>
                <h1 class="text-2xl font-bold text-green-600 tracking-tight" id="greetingName">yukNgaji</h1>
                <p class="text-xs text-gray-500 font-medium" id="welcomeMsg">Assalamu'alaikum, Ustadz!</p>
            </div>

            <div class="relative">
                <button onclick="toggleHeaderMenu()" id="btnHeaderMenu" class="relative text-gray-500 bg-white border border-gray-100 w-10 h-10 rounded-xl flex items-center justify-center active:scale-95 transition-transform shadow-sm">
                    <i class="fa-solid fa-ellipsis-vertical"></i>
                    <span id="headerMenuBadge" class="hidden absolute top-2 right-2.5 w-2 h-2 bg-red-500 rounded-full border border-white"></span>
                </button>

                <div id="dropdownHeaderMenu" class="hidden absolute right-0 top-full mt-2 bg-white border border-gray-100 rounded-2xl shadow-xl w-48 z-[60] overflow-hidden origin-top-right transition-all">
                    <div class="p-2 space-y-1">
                        <button onclick="openNotifModal(); toggleHeaderMenu();" class="w-full flex items-center gap-3 px-3 py-2.5 hover:bg-indigo-50 text-gray-600 transition-colors rounded-xl text-xs font-bold text-left relative">
                            <div class="w-8 h-8 rounded-lg bg-indigo-100 text-indigo-500 flex items-center justify-center shrink-0">
                                <i class="fa-solid fa-bell"></i>
                            </div>
                            <div class="flex-1">
                                <p>Pusat PR</p>
                            </div>
                            <span id="notifBadgeCounter" class="hidden bg-red-500 text-white text-[9px] font-black px-1.5 py-0.5 rounded-full">0</span>
                        </button>

                        <button onclick="openTransferDrawer(); toggleHeaderMenu();" class="w-full flex items-center gap-3 px-3 py-2.5 hover:bg-yellow-50 text-gray-600 transition-colors rounded-xl text-xs font-bold text-left">
                            <div class="w-8 h-8 rounded-lg bg-yellow-100 text-yellow-500 flex items-center justify-center shrink-0">
                                <i class="fa-solid fa-hand-holding-dollar"></i>
                            </div>
                            <div class="flex-1">
                                <p>Transfer Koin</p>
                            </div>
                        </button>

                        <div class="h-px bg-gray-100 my-1 mx-2"></div>

                        <button id="btnLogout" class="w-full flex items-center gap-3 px-3 py-2.5 hover:bg-red-50 text-gray-600 hover:text-red-600 transition-colors rounded-xl text-xs font-bold text-left">
                            <div class="w-8 h-8 rounded-lg bg-red-50 text-red-500 flex items-center justify-center shrink-0">
                                <i class="fa-solid fa-power-off"></i>
                            </div>
                            <div class="flex-1">
                                <p>Keluar (Logout)</p>
                            </div>
                        </button>
                    </div>
                </div>
            </div>
        </header>

        <section class="mb-16">
            <h2 class="text-xs font-bold text-gray-400 uppercase tracking-widest mb-3">Ringkasan Kelas</h2>
            <div class="grid grid-cols-2 gap-3">

                <div class="bg-green-50 p-4 rounded-2xl flex flex-col justify-center text-center border border-green-100 shadow-sm">
                    <div class="w-8 h-8 bg-green-100 text-green-500 rounded-full flex items-center justify-center mx-auto mb-2">
                        <i class="fa-solid fa-users"></i>
                    </div>
                    <h3 class="text-xl font-black text-green-700 mb-0.5" id="statStudents">-</h3>
                    <p class="text-[9px] text-green-600 font-bold uppercase tracking-widest">Total Anak</p>
                </div>

                <div class="bg-indigo-50 p-4 rounded-2xl flex flex-col justify-center text-center border border-indigo-100 shadow-sm">
                    <div class="w-8 h-8 bg-indigo-100 text-indigo-500 rounded-full flex items-center justify-center mx-auto mb-2">
                        <i class="fa-solid fa-file-contract"></i>
                    </div>
                    <h3 class="text-xl font-black text-indigo-600 mb-0.5" id="statExams">0</h3>
                    <p class="text-[9px] text-indigo-700 font-bold uppercase tracking-widest">Total Ujian</p>
                </div>

                <div onclick="openNotDoneModal()" class="col-span-2 bg-red-50 rounded-2xl p-4 border border-red-100 flex flex-col justify-between cursor-pointer hover:bg-red-100 transition-all active:scale-[0.98] group shadow-sm">
                    <div class="flex justify-between items-center mb-1">
                        <div class="flex items-center gap-3">
                            <div class="w-8 h-8 bg-red-100 text-red-600 rounded-lg flex items-center justify-center text-xs group-hover:scale-110 transition-transform">
                                <i class="fa-solid fa-user-clock"></i>
                            </div>
                            <span class="text-[11px] font-bold text-red-600 uppercase tracking-wider">Belum Mengerjakan PR / Ujian</span>
                        </div>
                        <i class="fa-solid fa-chevron-right text-xs text-red-300 group-hover:text-red-500 group-hover:translate-x-1 transition-all"></i>
                    </div>
                    <div class="flex items-end justify-between mt-2 pl-1">
                        <h3 class="text-3xl font-black text-red-700 leading-none" id="notDoneCount">0</h3>
                        <p class="text-[10px] text-red-400 font-bold">Tunggakan Aktif</p>
                    </div>
                </div>

            </div>
        </section>

        <div class="mb-6">
            <h2 class="text-xs font-bold text-gray-400 uppercase tracking-widest mb-3">Aksi Cepat</h2>

            <a href="/absensi" id="btnAbsensi" class="relative block w-full bg-blue-50 p-4 rounded-2xl overflow-hidden mb-3 active:scale-[0.98] transition-all">
                <div class="flex items-center justify-between z-10 relative">
                    <div class="flex items-center gap-3">
                        <div class="w-10 h-10 bg-blue-100 text-blue-500 rounded-xl flex items-center justify-center">
                            <i class="fa-solid fa-calendar-check text-lg"></i>
                        </div>
                        <div>
                            <h3 class="font-bold text-blue-800 text-sm">Absensi Hari Ini</h3>
                            <p id="absensiStatusText" class="text-[10px] text-blue-400 font-bold uppercase tracking-wider">Mengecek status...</p>
                        </div>
                    </div>
                    <i class="fa-solid fa-chevron-right text-blue-300 text-xs"></i>
                </div>
                <div id="absensiPulse" class="absolute inset-0 border-2 border-transparent rounded-2xl pointer-events-none"></div>
            </a>

            <div class="flex flex-col gap-2 sm:gap-3">
                <div class="grid grid-cols-4 gap-2 sm:gap-3">
                    <a href="/timer" class="relative bg-yellow-50 p-2 sm:p-4 rounded-2xl overflow-hidden active:scale-95 transition-transform flex flex-col items-center justify-center gap-1 sm:gap-2 text-center border border-yellow-100">
                        <div class="w-8 h-8 sm:w-10 sm:h-10 bg-yellow-100 text-yellow-600 rounded-xl flex items-center justify-center shrink-0">
                            <i class="fa-solid fa-stopwatch text-base sm:text-lg"></i>
                        </div>
                        <div>
                            <h3 class="font-bold text-yellow-800 text-[10px] sm:text-sm leading-tight">Timer</h3>
                            <p class="text-[7px] sm:text-[9px] text-yellow-500 font-bold uppercase tracking-wider truncate">Durasi</p>
                        </div>
                    </a>

                    <a href="/seats" class="relative bg-green-50 p-2 sm:p-4 rounded-2xl overflow-hidden active:scale-95 transition-transform flex flex-col items-center justify-center gap-1 sm:gap-2 text-center border border-green-100">
                        <div class="w-8 h-8 sm:w-10 sm:h-10 bg-green-100 text-green-600 rounded-xl flex items-center justify-center shrink-0">
                            <i class="fa-solid fa-chair text-base sm:text-lg"></i>
                        </div>
                        <div>
                            <h3 class="font-bold text-green-800 text-[10px] sm:text-sm leading-tight">Denah</h3>
                            <p class="text-[7px] sm:text-[9px] text-green-500 font-bold uppercase tracking-wider truncate">Posisi</p>
                        </div>
                    </a>

                    <a href="/hafalan" class="relative bg-emerald-50 p-2 sm:p-4 rounded-2xl overflow-hidden active:scale-95 transition-transform flex flex-col items-center justify-center gap-1 sm:gap-2 text-center border border-emerald-100">
                        <div class="w-8 h-8 sm:w-10 sm:h-10 bg-emerald-100 text-emerald-600 rounded-xl flex items-center justify-center shrink-0">
                            <i class="fa-solid fa-book-quran text-base sm:text-lg"></i>
                        </div>
                        <div>
                            <h3 class="font-bold text-emerald-800 text-[10px] sm:text-sm leading-tight">Hafalan</h3>
                            <p class="text-[7px] sm:text-[9px] text-emerald-500 font-bold uppercase tracking-wider truncate">Juz Amma</p>
                        </div>
                    </a>

                    <a href="/galeri" class="relative bg-purple-50 p-2 sm:p-4 rounded-2xl overflow-hidden active:scale-95 transition-transform flex flex-col items-center justify-center gap-1 sm:gap-2 text-center border border-purple-100 shadow-sm">
                        <div class="w-8 h-8 sm:w-10 sm:h-10 bg-purple-100 text-purple-600 rounded-xl flex items-center justify-center shrink-0">
                            <i class="fa-solid fa-images text-base sm:text-lg"></i>
                        </div>
                        <div>
                            <h3 class="font-bold text-purple-800 text-[10px] sm:text-sm leading-tight">Galeri</h3>
                            <p class="text-[7px] sm:text-[9px] text-purple-500 font-bold uppercase tracking-wider truncate">Foto Raport</p>
                        </div>
                    </a>
                </div>

                <div class="grid grid-cols-3 gap-2 sm:gap-3">
                    <button onclick="openDailyChallengeLinkModal()" class="relative bg-indigo-50 p-2 sm:p-4 rounded-2xl overflow-hidden active:scale-95 transition-transform flex flex-col items-center justify-center gap-1 sm:gap-2 text-center border border-indigo-100">
                        <div class="w-8 h-8 sm:w-10 sm:h-10 bg-indigo-100 text-indigo-600 rounded-xl flex items-center justify-center shrink-0">
                            <i class="fa-solid fa-keyboard text-base sm:text-lg"></i>
                        </div>
                        <div>
                            <h3 class="font-bold text-indigo-800 text-[10px] sm:text-sm leading-tight">Challenge</h3>
                            <p class="text-[7px] sm:text-[9px] text-indigo-500 font-bold uppercase tracking-wider truncate">Koin Harian</p>
                        </div>
                    </button>

                    <button onclick="openWrappedModal()" class="relative bg-pink-50 p-2 sm:p-4 rounded-2xl overflow-hidden active:scale-95 transition-transform flex flex-col items-center justify-center gap-1 sm:gap-2 text-center border border-pink-100 shadow-sm">
                        <div class="w-8 h-8 sm:w-10 sm:h-10 bg-pink-100 text-pink-600 rounded-xl flex items-center justify-center shrink-0">
                            <i class="fa-solid fa-gift text-base sm:text-lg"></i>
                        </div>
                        <div>
                            <h3 class="font-bold text-pink-800 text-[10px] sm:text-sm leading-tight">Wrapped</h3>
                            <p class="text-[7px] sm:text-[9px] text-pink-500 font-bold uppercase tracking-wider truncate">Kilas Balik</p>
                        </div>
                    </button>

                    <button onclick="copyShopLink()" class="relative bg-amber-50 p-2 sm:p-4 rounded-2xl overflow-hidden active:scale-95 transition-transform flex flex-col items-center justify-center gap-1 sm:gap-2 text-center border border-amber-100 shadow-sm group">
                        <div class="w-8 h-8 sm:w-10 sm:h-10 bg-amber-100 text-amber-600 rounded-xl flex items-center justify-center shrink-0">
                            <i class="fa-solid fa-store text-base sm:text-lg group-hover:animate-bounce"></i>
                        </div>
                        <div>
                            <h3 class="font-bold text-amber-800 text-[10px] sm:text-sm leading-tight">Toko</h3>
                            <p class="text-[7px] sm:text-[9px] text-amber-500 font-bold uppercase tracking-wider truncate">Salin Link</p>
                        </div>
                    </button>
                </div>
            </div>
        </div>

        <section class="flex-1">
            <div class="flex p-1 bg-gray-100 rounded-2xl mb-5 shadow-inner">
                <button id="tabList" class="flex-1 py-3 text-[10px] font-bold bg-white text-green-600 rounded-xl shadow-sm transition-all uppercase tracking-widest">
                    <i class="fa-solid fa-users mr-1"></i> Daftar Anak
                </button>
                <button id="tabLeaderboard" class="flex-1 py-3 text-[10px] font-bold text-gray-400 hover:text-gray-600 transition-all uppercase tracking-widest">
                    <i class="fa-solid fa-trophy mr-1"></i> Leaderboard
                </button>
            </div>

            <div id="contentList" class="block animate-slide-up">
                <div class="flex justify-between items-center mb-4 px-1">
                    <select id="filterList" class="bg-white border border-gray-200 text-gray-600 text-xs font-bold rounded-lg px-2 py-1.5 focus:ring-2 focus:ring-green-500 outline-none shadow-sm cursor-pointer">
                        <option value="abjad">Abjad (A - Z)</option>
                        <option value="nilai">Nilai Rata-rata Tertinggi</option>
                        <option value="rajin">Kehadiran Terbanyak</option>
                    </select>
                    <button class="text-xs font-bold text-green-600 hover:text-green-700 bg-green-50 px-3 py-1.5 rounded-lg transition-colors" onclick="window.location.href='/students'">Lihat Semua</button>
                </div>

                <div id="studentList" class="space-y-3">
                    <div class="text-center py-8"><i class="fa-solid fa-spinner fa-spin text-green-500 text-3xl"></i><p class="text-xs text-gray-400 mt-3 font-bold">Menghitung data...</p></div>
                </div>

                <div id="remainingStudentsContainer" class="mt-4 text-center hidden">
                    <span id="remainingStudentsText" class="inline-block bg-gray-100 text-gray-500 text-[10px] font-bold px-4 py-1.5 rounded-full border border-gray-200 shadow-sm">
                        + 5 anak lainnya
                    </span>
                </div>
            </div>

            <div id="contentLeaderboard" class="hidden animate-slide-up">
                <div class="flex items-end justify-center gap-2 mb-6 h-52 mt-6" id="podiumContainer">
                    <div class="text-center w-full"><i class="fa-solid fa-spinner fa-spin text-amber-500 text-2xl"></i></div>
                </div>

                <div class="bg-white rounded-3xl border border-gray-100 shadow-sm overflow-hidden mb-4">
                    <div class="bg-gray-50 px-4 py-3 border-b border-gray-100 flex items-center justify-between">
                        <h4 class="text-xs font-bold text-gray-700 uppercase tracking-widest"><i class="fa-solid fa-ranking-star text-amber-500 mr-1"></i> Top 5 Rata-rata</h4>
                    </div>
                    <div class="overflow-x-auto no-scrollbar">
                        <table class="w-full text-left border-collapse min-w-[300px]">
                            <thead>
                                <tr class="bg-white border-b border-gray-50 text-[9px] uppercase tracking-wider text-gray-400">
                                    <th class="p-3 font-bold">Nama</th>
                                    <th class="p-3 font-bold text-center">Tjw</th>
                                    <th class="p-3 font-bold text-center">Fqh</th>
                                    <th class="p-3 font-bold text-center">Thd</th>
                                    <th class="p-3 font-bold text-center">Hadir</th>
                                </tr>
                            </thead>
                            <tbody id="leaderboardTable" class="text-xs divide-y divide-gray-50">
                            </tbody>
                        </table>
                    </div>
                </div>
                <p class="text-center text-[9px] text-gray-400 italic mb-8">* Peringkat dihitung otomatis dari nilai kuesioner dan absensi.</p>
            </div>
        </section>

        <section class="mb-6">
            <div class="mt-8 mb-4 bg-white rounded-3xl border border-gray-100 shadow-sm overflow-hidden relative z-20">
                <button onclick="toggleDangerZone()" class="w-full px-5 py-4 flex items-center justify-between bg-gray-50 hover:bg-gray-100 transition-colors active:scale-95">
                    <div class="flex items-center gap-3">
                        <div class="w-8 h-8 bg-gray-200 text-gray-500 rounded-xl flex items-center justify-center">
                            <i class="fa-solid fa-gear"></i>
                        </div>
                        <span class="text-xs font-bold text-gray-700 uppercase tracking-widest">Pengaturan Lanjutan</span>
                    </div>
                    <i id="dangerIcon" class="fa-solid fa-chevron-down text-gray-400 transition-transform duration-300"></i>
                </button>

                <div id="dangerContent" class="hidden px-5 py-4 border-t border-gray-100 bg-white">

                    <!-- [BARU] SAKLAR MAINTENANCE -->
                    <div class="bg-amber-50 border border-amber-100 rounded-2xl p-4 mb-4 shadow-sm">
                        <div class="flex justify-between items-center mb-1">
                            <h4 class="text-sm font-bold text-amber-600"><i class="fa-solid fa-person-digging mr-1"></i> Mode Perbaikan</h4>
                            <label class="relative inline-flex items-center cursor-pointer">
                                <input type="checkbox" id="toggleMaintenance" class="sr-only peer" onchange="promptMaintenanceToggle(this)">
                                <div class="w-11 h-6 bg-gray-200 peer-focus:outline-none rounded-full peer peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:border-gray-300 after:border after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:bg-amber-500"></div>
                            </label>
                        </div>
                        <p class="text-[10px] text-amber-700 leading-relaxed font-medium">Aktifkan untuk memblokir akses murid ke halaman ujian/PR secara instan.</p>
                    </div>

                    <div class="bg-red-50 border border-red-100 rounded-2xl p-4 shadow-sm">
                        <h4 class="text-sm font-bold text-red-600 mb-1"><i class="fa-solid fa-triangle-exclamation mr-1"></i> Zona Bahaya</h4>
                        <p class="text-[10px] text-red-500 leading-relaxed mb-4 font-medium">Menghapus permanen seluruh data nilai, absen, tugas, dan kuesioner. Hanya nama siswa yang dipertahankan.</p>
                        <button onclick="openResetModal()" class="w-full py-3 bg-red-600 text-white text-xs font-bold rounded-xl shadow-lg shadow-red-600/20 active:scale-95 transition-all">
                            <i class="fa-solid fa-bomb mr-1"></i> Reset Tahun Ajaran
                        </button>
                    </div>
                </div>
            </div>
        </section>

        <div id="modalReset" class="fixed inset-0 bg-black/80 z-[100] hidden items-center justify-center p-4 backdrop-blur-sm">
            <div class="bg-white w-full max-w-sm rounded-[2rem] p-6 shadow-2xl animate-slide-up relative overflow-hidden">
                <div class="w-16 h-16 bg-red-50 text-red-600 rounded-full flex items-center justify-center text-3xl mx-auto mb-4 border border-red-100">
                    <i class="fa-solid fa-shield-halved"></i>
                </div>
                <h3 class="font-black text-gray-800 text-lg mb-1 text-center">Verifikasi Keamanan</h3>
                <p class="text-[10px] text-gray-500 mb-5 leading-relaxed text-center">
                    Masukkan <b>Password Akun</b> Anda untuk mengonfirmasi reset data. Sistem akan otomatis mengunduh file backup JSON.
                </p>
                <div class="bg-gray-50 p-1.5 rounded-xl border border-gray-200 mb-6">
                    <input type="password" id="resetPasswordInput" placeholder="Password Anda..." class="w-full px-3 py-2.5 bg-transparent text-sm font-bold text-gray-800 focus:outline-none text-center tracking-widest">
                </div>

                <div class="flex gap-2">
                    <button onclick="closeResetModal()" class="flex-1 py-3.5 bg-gray-100 text-gray-500 text-xs font-bold rounded-xl active:scale-95 transition-all">Batal</button>
                    <button id="btnExecuteReset" class="flex-1 py-3.5 bg-red-600 text-white text-xs font-bold rounded-xl shadow-lg active:scale-95 transition-all disabled:opacity-50 disabled:cursor-not-allowed" disabled>Hapus Data</button>
                </div>
            </div>
        </div>
    </div>

    <!-- MODAL TUNGGAKAN -->
    <div id="modalNotDone" class="fixed inset-0 bg-black/60 z-[100] hidden items-center justify-center p-4 backdrop-blur-sm">
        <div class="bg-white w-full max-w-sm rounded-[2rem] p-6 shadow-2xl flex flex-col max-h-[85vh] animate-slide-up">
            <div class="text-center mb-3">
                <div class="w-14 h-14 bg-red-50 text-red-500 rounded-full flex items-center justify-center text-2xl mx-auto mb-2 border border-red-100 shadow-inner">
                    <i class="fa-solid fa-user-clock"></i>
                </div>
                <h3 class="font-black text-gray-800 text-lg">Daftar Tunggakan</h3>
                <p class="text-[10px] font-bold text-gray-400 mt-1">Pantau PR dan ujian yang belum dikerjakan.</p>
            </div>

            <!-- TAB PR / UJIAN -->
            <div class="flex p-1 bg-gray-100 rounded-2xl mb-4 shadow-inner shrink-0">
                <button id="tabMissingPR" onclick="switchMissingTab('pr')" class="flex-1 py-2.5 text-[10px] font-bold bg-white text-red-600 rounded-xl shadow-sm transition-all uppercase tracking-widest">
                    <i class="fa-solid fa-book-open mr-1"></i> Belum PR
                </button>
                <button id="tabMissingExam" onclick="switchMissingTab('exam')" class="flex-1 py-2.5 text-[10px] font-bold text-gray-400 hover:text-gray-600 transition-all uppercase tracking-widest">
                    <i class="fa-solid fa-file-pen mr-1"></i> Belum Ujian
                </button>
            </div>

            <!-- FILTER PR -->
            <div id="missingPRFilters" class="flex gap-2 mb-4">
                <select id="modalFilterSubject" class="w-full text-xs bg-gray-50 border border-gray-200 text-gray-600 rounded-xl px-2 py-2 outline-none focus:ring-2 focus:ring-red-500 transition-all font-bold cursor-pointer">
                    <option value="">Semua Pelajaran</option>
                </select>
                <select id="modalFilterWeek" class="w-full text-xs bg-gray-50 border border-gray-200 text-gray-600 rounded-xl px-2 py-2 outline-none focus:ring-2 focus:ring-red-500 transition-all font-bold cursor-pointer">
                    <option value="">Semua Pertemuan</option>
                </select>
            </div>

            <!-- FILTER UJIAN -->
            <div id="missingExamFilters" class="hidden mb-4">
                <select id="modalFilterExamSubject" onchange="loadMissingExamList()" class="w-full text-xs bg-gray-50 border border-gray-200 text-gray-600 rounded-xl px-2 py-2 outline-none focus:ring-2 focus:ring-red-500 transition-all font-bold cursor-pointer">
                    <option value="">Semua Mapel Ujian</option>
                    <option value="tajwid">Tajwid</option>
                    <option value="fiqih">Fiqih</option>
                    <option value="tauhid">Tauhid</option>
                </select>
            </div>

            <div id="notDoneListContainer" class="flex-1 overflow-y-auto space-y-2 pr-1 mb-4 custom-scrollbar">
                <div class="text-center py-6"><i class="fa-solid fa-spinner fa-spin text-red-500"></i></div>
            </div>

            <button onclick="closeNotDoneModal()" class="w-full py-3.5 bg-red-500 hover:bg-red-600 text-white font-bold rounded-2xl active:scale-95 transition-all text-sm">
                Tutup
            </button>
        </div>
    </div>

    <!-- PUSAT PR (NOTIFIKASI + LEADERBOARD) -->
    <div id="modalNotif" class="fixed inset-0 bg-black/60 z-[200] hidden items-end sm:items-center justify-center p-0 sm:p-4 backdrop-blur-sm transition-opacity opacity-0">
        <div class="bg-white w-full sm:max-w-md rounded-t-[2rem] sm:rounded-[2rem] p-6 shadow-2xl flex flex-col h-[85vh] sm:h-[80vh] transform translate-y-full sm:translate-y-0 transition-transform duration-300" id="notifBox">

            <div class="flex justify-between items-center mb-4 shrink-0">
                <div class="flex items-center gap-2">
                    <div class="w-10 h-10 bg-indigo-100 text-indigo-600 rounded-full flex items-center justify-center">
                        <i class="fa-solid fa-bell"></i>
                    </div>
                    <h3 class="font-black text-gray-800 text-lg">Pusat PR</h3>
                </div>
                <button onclick="closeNotifModal()" class="w-8 h-8 bg-gray-100 text-gray-400 rounded-full flex items-center justify-center active:scale-95"><i class="fa-solid fa-xmark"></i></button>
            </div>

            <div class="flex p-1 bg-gray-100 rounded-2xl mb-4 shadow-inner shrink-0">
                <button id="tabNotifListBtn" onclick="switchNotifTab('list')" class="flex-1 py-2.5 text-[10px] font-bold bg-white text-indigo-600 rounded-xl shadow-sm transition-all uppercase tracking-widest">
                    <i class="fa-solid fa-inbox mr-1"></i> Pesan Baru
                </button>
                <button id="tabNotifLBBtn" onclick="switchNotifTab('lb')" class="flex-1 py-2.5 text-[10px] font-bold text-gray-400 hover:text-gray-600 transition-all uppercase tracking-widest">
                    <i class="fa-solid fa-trophy mr-1"></i> Leaderboard
                </button>
            </div>

            <!-- CONTENT: NOTIF LIST -->
            <div id="contentNotifList" class="flex-1 flex flex-col overflow-hidden">
                <div class="flex justify-between items-center mb-3 pb-3 border-b border-gray-100 shrink-0">
                    <p class="text-[10px] font-bold text-gray-400 uppercase tracking-widest"><span id="notifCountText">0</span> Pesan Baru</p>
                    <button onclick="clearAllNotifs()" class="text-[10px] font-bold text-red-500 hover:text-red-600 bg-red-50 px-3 py-1.5 rounded-lg active:scale-95 transition-colors">
                        <i class="fa-solid fa-trash-can mr-1"></i> Hapus Semua
                    </button>
                </div>

                <div id="notifListContainer" class="flex-1 overflow-y-auto space-y-3 custom-scrollbar pr-1 pb-6">
                    <div class="text-center py-10"><i class="fa-solid fa-spinner fa-spin text-indigo-500"></i></div>
                </div>
            </div>

            <!-- CONTENT: LEADERBOARD PR -->
            <div id="contentNotifLB" class="hidden flex-1 flex flex-col overflow-hidden">
                <div class="grid grid-cols-1 gap-2 mb-3 shrink-0">
                    <select id="lbMode" onchange="handleNotifLeaderboardModeChange()" class="w-full text-xs bg-gray-50 border border-gray-200 text-gray-600 rounded-xl px-2 py-2.5 outline-none focus:ring-2 focus:ring-indigo-500 font-bold cursor-pointer">
                        <option value="pr">Leaderboard PR</option>
                        <option value="exam">Leaderboard Ujian</option>
                    </select>

                    <div id="lbPRFilters" class="flex gap-2">
                        <select id="lbFilterSubject" onchange="loadPRLeaderboard()" class="w-full text-xs bg-gray-50 border border-gray-200 text-gray-600 rounded-xl px-2 py-2.5 outline-none focus:ring-2 focus:ring-indigo-500 font-bold cursor-pointer">
                            <option value="tajwid">Tajwid</option>
                            <option value="fiqih">Fiqih</option>
                            <option value="tauhid">Tauhid</option>
                        </select>
                        <select id="lbFilterWeek" onchange="loadPRLeaderboard()" class="w-full text-xs bg-gray-50 border border-gray-200 text-gray-600 rounded-xl px-2 py-2.5 outline-none focus:ring-2 focus:ring-indigo-500 font-bold cursor-pointer">
                        </select>
                    </div>

                    <div id="lbExamFilters" class="hidden flex gap-2">
                        <select id="lbExamSubject" onchange="loadExamOptionsForLB()" class="w-full text-xs bg-gray-50 border border-gray-200 text-gray-600 rounded-xl px-2 py-2.5 outline-none focus:ring-2 focus:ring-indigo-500 font-bold cursor-pointer">
                            <option value="tajwid">Tajwid</option>
                            <option value="fiqih">Fiqih</option>
                            <option value="tauhid">Tauhid</option>
                        </select>

                        <select id="lbExamId" onchange="loadPRLeaderboard()" class="w-full text-xs bg-gray-50 border border-gray-200 text-gray-600 rounded-xl px-2 py-2.5 outline-none focus:ring-2 focus:ring-indigo-500 font-bold cursor-pointer">
                            <option value="">Memuat ujian...</option>
                        </select>
                    </div>
                </div>
                <div id="lbPRContainer" class="flex-1 overflow-y-auto space-y-2 custom-scrollbar pr-1 pb-4">
                </div>
            </div>

        </div>
    </div>

    <!-- MODAL DETAIL NOTIFIKASI PR -->
    <div id="modalNotifDetail" class="fixed inset-0 bg-black/80 z-[300] hidden items-center justify-center p-4 backdrop-blur-sm transition-opacity duration-300 opacity-0">
        <div class="bg-white w-full max-w-sm rounded-[2rem] p-6 shadow-2xl relative overflow-hidden transform scale-95 transition-transform duration-300" id="notifDetailBox">
            <button onclick="closeNotifDetail()" class="absolute top-4 right-4 w-8 h-8 bg-gray-100 text-gray-500 rounded-full flex items-center justify-center active:scale-95 z-10"><i class="fa-solid fa-xmark"></i></button>
            <h3 class="font-black text-gray-800 text-lg mb-4 text-center border-b border-gray-100 pb-3">Detail Laporan PR</h3>
            <div id="notifDetailContent" class="space-y-4 hidden">
                <div class="w-32 h-32 mx-auto rounded-2xl bg-gray-100 overflow-hidden shadow-inner border-4 border-white shadow-md relative">
                    <img id="detailSatpamPhoto" src="" class="w-full h-full object-cover" />
                    <div id="detailSatpamPlaceholder" class="absolute inset-0 flex flex-col items-center justify-center text-gray-400 bg-gray-50 hidden">
                        <i class="fa-solid fa-user-shield text-3xl mb-1"></i>
                        <span class="text-[8px] font-bold uppercase tracking-widest text-center mt-2 px-2">Tidak Ada<br>Foto Bukti</span>
                    </div>
                </div>
                <div class="text-center">
                    <h4 id="detailStudentName" class="font-black text-xl text-gray-800 leading-tight">Nama Siswa</h4>
                    <p id="detailSubjectWeek" class="text-[10px] font-bold text-indigo-600 bg-indigo-50 inline-block px-3 py-1 rounded-lg mt-2 uppercase tracking-widest border border-indigo-100">MAPEL • MINGGU</p>
                </div>
                <div class="bg-gray-50 rounded-2xl p-4 text-center border border-gray-100 shadow-sm mt-2">
                    <p class="text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-1">Nilai Pengerjaan</p>
                    <p id="detailScore" class="text-5xl font-black text-gray-800 drop-shadow-sm">0</p>
                </div>
            </div>
            <div id="notifDetailLoading" class="py-10 flex flex-col items-center justify-center text-indigo-500">
                <i class="fa-solid fa-spinner fa-spin text-3xl mb-3"></i>
                <p class="text-xs font-bold text-gray-500">Menarik data dari server...</p>
            </div>
        </div>
    </div>

    <!-- [BARU] MODAL KONFIRMASI GLOBAL DINAMIS -->
    <div id="modalConfirmGlobal" class="fixed inset-0 bg-black/60 z-[9999] hidden items-center justify-center p-4 backdrop-blur-sm transition-opacity duration-300 opacity-0">
        <div class="bg-white w-full max-w-sm rounded-[2rem] p-6 shadow-2xl text-center transform scale-100 animate-bounce-short border-4 border-indigo-400 transition-all duration-300" id="modalConfirmGlobalBox">
            <div id="confirmGlobalIconBox" class="w-20 h-20 bg-indigo-50 text-indigo-500 rounded-full flex items-center justify-center text-4xl mx-auto mb-4 border border-indigo-100 shadow-inner transition-all duration-300">
                <i id="confirmGlobalIcon" class="fa-solid fa-circle-question"></i>
            </div>
            <h3 id="confirmGlobalTitle" class="font-black text-gray-800 text-xl mb-2">Konfirmasi</h3>
            <p id="confirmGlobalMessage" class="text-sm text-gray-600 mb-6 leading-relaxed font-medium">Apakah Anda yakin?</p>
            <div class="flex gap-3">
                <button id="btnConfirmGlobalCancel" class="flex-1 py-3.5 bg-gray-100 hover:bg-gray-200 text-gray-500 font-bold rounded-2xl active:scale-95 transition-all text-sm">Batal</button>
                <button id="btnConfirmGlobalYes" class="flex-1 py-3.5 bg-indigo-600 hover:bg-indigo-700 text-white font-bold rounded-2xl shadow-lg shadow-indigo-600/30 active:scale-95 transition-all text-sm">Ya, Lanjutkan</button>
            </div>
        </div>
    </div>

    <!-- DRAWER TRANSFER KOIN -->
    <div id="drawerTransfer" class="fixed inset-0 bg-black/60 z-[120] hidden items-end justify-center p-0 backdrop-blur-sm transition-all">
        <div class="bg-white w-full max-w-md rounded-t-[2rem] shadow-2xl flex flex-col animate-slide-up-drawer relative overflow-hidden pb-8 max-h-[85vh]">
            <div class="w-full flex justify-center pt-3 pb-1 bg-white z-20">
                <div class="w-12 h-1.5 bg-gray-200 rounded-full"></div>
            </div>
            <div class="px-6 pt-4 pb-4 border-b border-gray-100 flex justify-between items-center bg-white">
                <div>
                    <h3 class="font-black text-gray-800 text-lg"><i class="fa-solid fa-coins text-yellow-500 mr-2"></i> Transfer Koin</h3>
                    <p class="text-[10px] text-gray-500 font-bold mt-1">Beri reward ke siswa yang bisa jawab</p>
                </div>
                <button onclick="closeTransferDrawer()" class="w-8 h-8 bg-gray-100 text-gray-500 rounded-full flex items-center justify-center active:scale-95">
                    <i class="fa-solid fa-xmark"></i>
                </button>
            </div>
            <div class="p-6 overflow-y-auto no-scrollbar">
                <label class="text-[10px] font-black text-gray-400 uppercase tracking-widest mb-2 block">1. Pilih Murid</label>
                <div class="relative mb-6">
                    <select id="transferStudentSelect" class="w-full bg-gray-50 border border-gray-200 text-gray-700 text-sm font-bold rounded-xl pl-4 pr-10 py-3.5 outline-none focus:border-yellow-500 focus:ring-2 focus:ring-yellow-500/20 transition-all appearance-none cursor-pointer">
                        <option value="">⏳ Memuat data...</option>
                    </select>
                    <i class="fa-solid fa-chevron-down absolute right-4 top-1/2 -translate-y-1/2 text-gray-400 pointer-events-none"></i>
                </div>
                <label class="text-[10px] font-black text-gray-400 uppercase tracking-widest mb-2 block">2. Tentukan Jumlah Koin</label>
                <div class="grid grid-cols-4 gap-2 mb-3">
                    <button onclick="setTransferAmount(20)" class="py-2.5 bg-white text-yellow-600 font-black text-xs rounded-xl border border-yellow-200 hover:bg-yellow-50 active:scale-95 transition-all shadow-sm">+20</button>
                    <button onclick="setTransferAmount(50)" class="py-2.5 bg-white text-yellow-600 font-black text-xs rounded-xl border border-yellow-200 hover:bg-yellow-50 active:scale-95 transition-all shadow-sm">+50</button>
                    <button onclick="setTransferAmount(100)" class="py-2.5 bg-white text-yellow-600 font-black text-xs rounded-xl border border-yellow-200 hover:bg-yellow-50 active:scale-95 transition-all shadow-sm">+100</button>
                    <button onclick="setTransferAmount(200)" class="py-2.5 bg-white text-yellow-600 font-black text-xs rounded-xl border border-yellow-200 hover:bg-yellow-50 active:scale-95 transition-all shadow-sm">+200</button>
                </div>
                <div class="relative mb-8">
                    <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
                        <i class="fa-solid fa-coins text-yellow-400"></i>
                    </div>
                    <input type="number" id="transferAmountInput" placeholder="Atau ketik manual..." class="w-full bg-white border-2 border-gray-200 text-gray-700 text-sm font-black rounded-xl pl-10 pr-4 py-3.5 outline-none focus:border-yellow-500 focus:ring-2 focus:ring-yellow-500/20 transition-all">
                </div>
                <button id="btnSubmitTransfer" onclick="submitTransfer()" class="w-full py-4 bg-yellow-500 hover:bg-yellow-600 text-white font-black rounded-2xl shadow-lg shadow-yellow-500/30 active:translate-y-[2px] transition-all flex justify-center items-center gap-2">
                    <i class="fa-solid fa-paper-plane"></i> Kirim Reward Sekarang
                </button>
            </div>
        </div>
    </div>

    <div id="modalDailyChallengeLink" class="fixed inset-0 bg-black/60 z-[220] hidden items-center justify-center p-4 backdrop-blur-sm">
        <div class="bg-white w-full max-w-sm rounded-[2rem] p-6 shadow-2xl animate-slide-up text-center border border-indigo-100">
            <div class="w-16 h-16 bg-indigo-50 text-indigo-600 rounded-2xl flex items-center justify-center text-3xl mx-auto mb-4 border border-indigo-100 shadow-inner">
                <i class="fa-solid fa-keyboard"></i>
            </div>

            <h3 class="font-black text-gray-800 text-lg mb-2">Daily Challenge</h3>
            <p class="text-xs font-bold text-gray-500 leading-relaxed mb-6">
                Pilih mode link. Simulasi untuk ustadz, real untuk murid.
            </p>

            <div class="grid grid-cols-2 gap-3">
                <button onclick="copyDailyChallengeLink('preview')" class="py-4 bg-amber-50 text-amber-600 border border-amber-100 rounded-2xl font-black text-xs active:scale-95 transition-all">
                    <i class="fa-solid fa-eye mb-2 block text-xl"></i>
                    Simulasi
                </button>

                <button onclick="copyDailyChallengeLink('real')" class="py-4 bg-indigo-50 text-indigo-600 border border-indigo-100 rounded-2xl font-black text-xs active:scale-95 transition-all">
                    <i class="fa-solid fa-user-graduate mb-2 block text-xl"></i>
                    Real Murid
                </button>
            </div>

            <button onclick="closeDailyChallengeLinkModal()" class="mt-5 text-[10px] font-black text-gray-400 hover:text-gray-600 underline">
                Tutup
            </button>
        </div>
    </div>

    <div id="modalWrapped" class="fixed inset-0 bg-black/60 z-[220] hidden items-center justify-center p-4 backdrop-blur-sm">
        <div class="bg-white w-full max-w-sm rounded-[2rem] p-6 shadow-2xl flex flex-col max-h-[85vh] animate-slide-up border border-pink-100">
            <div class="flex justify-between items-center mb-4">
                <div class="flex items-center gap-3">
                     <div class="w-10 h-10 bg-pink-100 text-pink-600 rounded-xl flex items-center justify-center text-lg"><i class="fa-solid fa-gift"></i></div>
                     <div>
                         <h3 class="font-black text-gray-800 text-lg leading-tight">Lihat Wrapped</h3>
                         <p class="text-[9px] font-bold text-gray-400 uppercase tracking-widest">Pilih siswa</p>
                     </div>
                </div>
                <button onclick="closeWrappedModal()" class="w-8 h-8 bg-gray-100 text-gray-400 rounded-full flex items-center justify-center active:scale-95"><i class="fa-solid fa-xmark"></i></button>
            </div>

            <div id="wrappedStatsContainer" class="bg-gray-50 py-2.5 rounded-xl text-center text-[10px] mb-4 border border-gray-100">
                </div>

            <div class="relative mb-4 shrink-0">
                <i class="fa-solid fa-search absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 text-xs"></i>
                <input type="text" id="searchWrappedStudent" onkeyup="filterWrappedStudents()" placeholder="Cari nama siswa..." class="w-full bg-gray-50 border border-gray-200 text-xs font-bold rounded-xl pl-9 pr-3 py-2.5 focus:outline-none focus:border-pink-300 focus:ring-2 focus:ring-pink-100 transition-all">
            </div>

            <div id="wrappedStudentList" class="flex-1 overflow-y-auto grid grid-cols-2 gap-3 custom-scrollbar pr-1 mb-2">
                </div>
        </div>
    </div>

    <Toast />
    <ModalAlert />
    <NavbarGuru activeMenu="home" />
</Layout>

<script>
    const API_URL = import.meta.env.PUBLIC_API_URL || 'http://localhost:3000';
    const token = localStorage.getItem('token');
    const userData = JSON.parse(localStorage.getItem('user') || '{}');

    let enrichedStudents = [];
    let availableTasks = [];

    let missingTab = 'pr';
    let missingExamCache = {};

    if (!token) window.location.replace('/');

    const greetingEl = document.getElementById('greetingName');
    if (greetingEl) greetingEl.innerText = `Ahlan, Ustadz ${userData.username || ''}!`;

    // --- TAB SWITCHER LOGIC (Ringkasan Kelas) ---
    const tabList = document.getElementById('tabList');
    const tabLeader = document.getElementById('tabLeaderboard');
    const contentList = document.getElementById('contentList');
    const contentLeader = document.getElementById('contentLeaderboard');

    const activeTabClass = "flex-1 py-3 text-[10px] font-bold bg-white text-green-600 rounded-xl shadow-sm transition-all uppercase tracking-widest";
    const inactiveTabClass = "flex-1 py-3 text-[10px] font-bold text-gray-400 hover:text-gray-600 transition-all uppercase tracking-widest";

    tabList.onclick = () => {
        tabList.className = activeTabClass; tabLeader.className = inactiveTabClass;
        contentList.classList.replace('hidden', 'block');
        contentLeader.classList.replace('block', 'hidden');
    };
    tabLeader.onclick = () => {
        tabLeader.className = activeTabClass; tabList.className = inactiveTabClass;
        contentLeader.classList.replace('hidden', 'block'); contentList.classList.replace('block', 'hidden');
    };

    window.openWrappedModal = () => {
        document.getElementById('modalWrapped').classList.replace('hidden', 'flex');
        renderWrappedStudentList('');
    };

    window.closeWrappedModal = () => {
        document.getElementById('modalWrapped').classList.replace('flex', 'hidden');
        document.getElementById('searchWrappedStudent').value = '';
    };

    window.filterWrappedStudents = () => {
        const keyword = document.getElementById('searchWrappedStudent').value.toLowerCase();
        renderWrappedStudentList(keyword);
    };

    window.renderWrappedStudentList = (keyword = '') => {
        const container = document.getElementById('wrappedStudentList');
        const statsContainer = document.getElementById('wrappedStatsContainer');

        // 1. Kumpulkan dulu data anak yang SUDAH SELESAI semua ujian
        const completedStudents = enrichedStudents.filter(s => s.is_exam_completed === true);

        // 2. Hitung yang belum selesai untuk info di Card Stats
        const pendingCount = enrichedStudents.length - completedStudents.length;

        // Render Stats Card (Tetap tampilkan jumlah yang belum)
        if (statsContainer) {
            statsContainer.className = "mb-4";
            statsContainer.innerHTML = `
                <div class="grid grid-cols-2 gap-2">
                    <div class="bg-green-50 border border-green-100 p-3 rounded-2xl flex items-center justify-between shadow-sm transition-all hover:bg-green-100">
                        <div class="flex items-center gap-2.5">
                            <div class="w-7 h-7 bg-green-100 text-green-600 rounded-xl flex items-center justify-center text-[10px] shadow-inner">
                                <i class="fa-solid fa-check"></i>
                            </div>
                            <span class="text-[10px] font-bold text-green-700 uppercase tracking-widest">Selesai</span>
                        </div>
                        <span class="text-lg font-black text-green-700 drop-shadow-sm">${completedStudents.length}</span>
                    </div>

                    <div class="bg-red-50 border border-red-100 p-3 rounded-2xl flex items-center justify-between shadow-sm transition-all hover:bg-red-100">
                        <div class="flex items-center gap-2.5">
                            <div class="w-7 h-7 bg-red-100 text-red-500 rounded-xl flex items-center justify-center text-[10px] shadow-inner">
                                <i class="fa-solid fa-lock"></i>
                            </div>
                            <span class="text-[10px] font-bold text-red-700 uppercase tracking-widest">Belum</span>
                        </div>
                        <span class="text-lg font-black text-red-600 drop-shadow-sm">${pendingCount}</span>
                    </div>
                </div>
            `;
        }

        // 3. Filter HANYA anak yang sudah selesai berdasarkan keyword pencarian
        const filtered = completedStudents.filter(s => s.name.toLowerCase().includes(keyword));

        if(filtered.length === 0) {
            container.innerHTML = '<p class="text-center text-xs text-gray-400 italic py-8 col-span-2">Belum ada siswa yang menyelesaikan ujian.</p>';
            return;
        }

        // 4. Render list (otomatis hanya berisi anak yang sudah selesai, list anak yang di-disable dihilangkan)
        container.innerHTML = filtered.map(s => {
            const photoHtml = s.photo_url
                ? `<img src="${s.photo_url}" class="w-full h-full object-cover">`
                : `<span class="text-[10px] font-black text-gray-400">${s.name.substring(0,2).toUpperCase()}</span>`;

            return `
                <div class="w-full flex items-center justify-between p-2 sm:p-2.5 rounded-xl border border-gray-200 bg-white hover:bg-pink-50 hover:border-pink-200 transition-all group overflow-hidden shadow-sm">

                    <button onclick="goToWrapped('${s.id}')" class="flex items-center gap-2.5 min-w-0 flex-1 text-left cursor-pointer active:scale-[0.98]">
                        <div class="w-8 h-8 sm:w-9 sm:h-9 bg-gray-100 rounded-full flex items-center justify-center overflow-hidden shrink-0 border border-white shadow-sm">
                            ${photoHtml}
                        </div>
                        <span class="block text-[10px] sm:text-xs font-bold text-gray-700 group-hover:text-pink-600 truncate w-full">${s.name.split(' ')[0]}</span>
                    </button>

                    <div class="shrink-0 pl-2 ml-1 border-l border-gray-100 flex items-center justify-center">
                        <button onclick="shareWrappedLink('${s.id}', '${s.name}', event)" class="w-7 h-7 flex items-center justify-center rounded-lg bg-slate-50 text-slate-400 hover:bg-green-500 hover:text-white transition-colors active:scale-90" title="Salin Link Anak">
                            <i class="fa-solid fa-link text-[10px]"></i>
                        </button>
                    </div>

                </div>
            `;
        }).join('');
    };

    window.copyShopLink = () => {
        // Ambil token dari localStorage atau sesuai metode yang kamu gunakan di dashboard
        const token = localStorage.getItem('token');

        // Asumsi rute halamannya adalah /toko-kenangan
        const shopUrl = `${window.location.origin}/toko-kenangan?token=${token}`;

        navigator.clipboard.writeText(shopUrl).then(() => {
            if (window.showToastAlert) {
                window.showToastAlert('success', 'Tersalin!', 'Link Toko Kenangan berhasil disalin. Silakan bagikan ke murid!');
            } else {
                alert('Link Toko Kenangan berhasil disalin!');
            }
        }).catch(() => {
            if (window.showToastAlert) {
                window.showToastAlert('danger', 'Gagal', 'Browser tidak mendukung copy otomatis.');
            } else {
                alert('Gagal menyalin link.');
            }
        });
    };

    window.shareWrappedLink = async (studentId, studentName, event) => {
        // Mencegah agar saat tombol link diklik, Ustadz tidak ikut masuk ke halaman wrapped
        event.stopPropagation();

        const origin = window.location.origin;

        // [PERBAIKAN] Tambahkan &view_only=true agar murid langsung masuk mode arsip/galeri
        const link = `${origin}/wrapped/${studentId}?token=${token}&view_only=true`;

        // Pesan yang akan disalin
        const text = `Assalamu'alaikum ${studentName}! 🎉\n\nYuk lihat kilas balik perjalanan dan rapor Ngaji kamu selama ini di *Ngaji Wrapped*!\n\nKlik link di bawah ini ya:\n${link}`;

        try {
            await navigator.clipboard.writeText(text);
            window.showToast?.('success', 'Link Tersalin!', `Link Wrapped khusus untuk ${studentName} siap di-paste ke WA.`);
        } catch (err) {
            window.showToast?.('danger', 'Gagal', 'Gagal menyalin link.');
        }
    };

    window.goToWrapped = (studentId) => {
        // Rahasianya ada di parameter &view_only=true
        const url = `/wrapped/${studentId}?token=${token}&view_only=true`;
        window.open(url, '_blank'); // Buka di tab baru
    };

    // ==============================================================
    // [BARU] LOGIKA MODAL KONFIRMASI GLOBAL
    // ==============================================================
    let globalConfirmCallback = null;
    let globalKeepIfCancel = false;

    window.showConfirmGlobal = (title, message, onConfirm, warnIfCancel = false, type = 'primary') => {
        document.getElementById('confirmGlobalTitle').innerText = title;
        document.getElementById('confirmGlobalMessage').innerHTML = message;
        globalConfirmCallback = onConfirm;
        globalKeepIfCancel = warnIfCancel;

        const modalBox = document.getElementById('modalConfirmGlobalBox');
        const iconBox = document.getElementById('confirmGlobalIconBox');
        const icon = document.getElementById('confirmGlobalIcon');
        const btnYes = document.getElementById('btnConfirmGlobalYes');

        if (type === 'danger') {
            modalBox.className = 'bg-white w-full max-w-sm rounded-[2rem] p-6 shadow-2xl text-center transform scale-100 animate-bounce-short border-4 border-red-400 transition-all duration-300';
            iconBox.className = 'w-20 h-20 bg-red-50 text-red-500 rounded-full flex items-center justify-center text-4xl mx-auto mb-4 border border-red-100 shadow-inner transition-all duration-300';
            icon.className = 'fa-solid fa-triangle-exclamation';
            btnYes.className = 'flex-1 py-3.5 bg-red-600 hover:bg-red-700 text-white font-bold rounded-2xl shadow-lg shadow-red-600/30 active:scale-95 transition-all text-sm';
        } else if (type === 'warning') {
            modalBox.className = 'bg-white w-full max-w-sm rounded-[2rem] p-6 shadow-2xl text-center transform scale-100 animate-bounce-short border-4 border-amber-400 transition-all duration-300';
            iconBox.className = 'w-20 h-20 bg-amber-50 text-amber-500 rounded-full flex items-center justify-center text-4xl mx-auto mb-4 border border-amber-100 shadow-inner transition-all duration-300';
            icon.className = 'fa-solid fa-person-digging';
            btnYes.className = 'flex-1 py-3.5 bg-amber-500 hover:bg-amber-600 text-white font-bold rounded-2xl shadow-lg shadow-amber-500/30 active:scale-95 transition-all text-sm';
        } else {
            modalBox.className = 'bg-white w-full max-w-sm rounded-[2rem] p-6 shadow-2xl text-center transform scale-100 animate-bounce-short border-4 border-indigo-400 transition-all duration-300';
            iconBox.className = 'w-20 h-20 bg-indigo-50 text-indigo-500 rounded-full flex items-center justify-center text-4xl mx-auto mb-4 border border-indigo-100 shadow-inner transition-all duration-300';
            icon.className = 'fa-solid fa-circle-question';
            btnYes.className = 'flex-1 py-3.5 bg-indigo-600 hover:bg-indigo-700 text-white font-bold rounded-2xl shadow-lg shadow-indigo-600/30 active:scale-95 transition-all text-sm';
        }

        const modal = document.getElementById('modalConfirmGlobal');
        modal.classList.replace('hidden', 'flex');
        setTimeout(() => modal.classList.replace('opacity-0', 'opacity-100'), 10);
    };

    document.getElementById('btnConfirmGlobalCancel').onclick = () => {
        const modal = document.getElementById('modalConfirmGlobal');
        modal.classList.replace('opacity-100', 'opacity-0');
        setTimeout(() => modal.classList.replace('flex', 'hidden'), 300);
        globalConfirmCallback = null;
        if (globalKeepIfCancel) {
            window.showToast?.('info', 'Dibatalkan', 'Tindakan telah dibatalkan.');
        }
    };

    document.getElementById('btnConfirmGlobalYes').onclick = () => {
        const modal = document.getElementById('modalConfirmGlobal');
        modal.classList.replace('opacity-100', 'opacity-0');
        setTimeout(() => modal.classList.replace('flex', 'hidden'), 300);
        if (globalConfirmCallback) globalConfirmCallback();
    };


    // ==============================================================
    // LOGIKA MENU HEADER (TITIK TIGA)
    // ==============================================================
    window.toggleHeaderMenu = () => {
        const menu = document.getElementById('dropdownHeaderMenu');
        menu.classList.toggle('hidden');

        const closeMenu = (e) => {
            const btn = document.getElementById('btnHeaderMenu');
            if (!menu.contains(e.target) && !btn.contains(e.target)) {
                menu.classList.add('hidden');
                document.removeEventListener('click', closeMenu);
            }
        };

        if (!menu.classList.contains('hidden')) {
            setTimeout(() => document.addEventListener('click', closeMenu), 10);
        }
    };


    // ==============================================================
    // LOGIKA NOTIFIKASI PR (GROUPING, AUTO-DELETE, DETAIL, LEADERBOARD)
    // ==============================================================
    let notifications = [];

    // TABS NOTIFIKASI
    window.switchNotifTab = (mode) => {
        const btnList = document.getElementById('tabNotifListBtn');
        const btnLB = document.getElementById('tabNotifLBBtn');
        const contentList = document.getElementById('contentNotifList');
        const contentLB = document.getElementById('contentNotifLB');

        const activeCls = "flex-1 py-2.5 text-[10px] font-bold bg-white text-indigo-600 rounded-xl shadow-sm transition-all uppercase tracking-widest";
        const inactiveCls = "flex-1 py-2.5 text-[10px] font-bold text-gray-400 hover:text-gray-600 transition-all uppercase tracking-widest";

        if (mode === 'list') {
            btnList.className = activeCls;
            btnLB.className = inactiveCls;
            contentList.classList.replace('hidden', 'flex');
            contentLB.classList.replace('flex', 'hidden');
        } else {
            btnLB.className = activeCls;
            btnList.className = inactiveCls;
            contentLB.classList.replace('hidden', 'flex');
            contentList.classList.replace('flex', 'hidden');
            loadPRLeaderboard();
        }
    };

    const getTimeCategory = (dateString) => {
        const notifDate = new Date(dateString);
        const now = new Date();

        const todayMidnight = new Date(now.getFullYear(), now.getMonth(), now.getDate());
        const notifMidnight = new Date(notifDate.getFullYear(), notifDate.getMonth(), notifDate.getDate());

        const diffDays = Math.floor((todayMidnight - notifMidnight) / (1000 * 60 * 60 * 24));

        if (diffDays === 0) {
            const hour = notifDate.getHours();
            if (hour < 12) return "Pagi Ini";
            if (hour < 15) return "Siang Ini";
            if (hour < 18) return "Sore Ini";
            return "Malam Ini";
        }
        if (diffDays === 1) return "Kemarin";
        if (diffDays > 1 && diffDays <= 7) return "Minggu Ini";
        return "Beberapa Minggu Lalu";
    };

    const loadNotifications = async (isFirstLoad = false) => {
        try {
            const res = await fetch(`${API_URL}/api/notifications`, { headers: { 'Authorization': `Bearer ${token}` }});
            const result = await res.json();
            notifications = result.data || [];

            if (!isFirstLoad) {
                const oldNotifs = notifications.filter(n => {
                    const diffDays = Math.floor((new Date() - new Date(n.created_at)) / (1000 * 60 * 60 * 24));
                    return diffDays > 7;
                });

                if (oldNotifs.length > 0) {
                    showConfirmGlobal(
                        "Bersih-bersih Laci",
                        `Terdapat ${oldNotifs.length} notif yang sudah lewat dari seminggu. Mau dihapus biar laci rapi?`,
                        async () => {
                            const oldIds = oldNotifs.map(n => n.id);
                            notifications = notifications.filter(n => !oldIds.includes(n.id));
                            updateNotifBadge();
                            renderNotifications();
                            try {
                                await Promise.all(oldIds.map(id => fetch(`${API_URL}/api/notifications/${id}`, { method: 'DELETE', headers: { 'Authorization': `Bearer ${token}` }})));
                            } catch(e) {}
                        },
                        true,
                        'primary'
                    );
                }
            }

            updateNotifBadge();
            renderNotifications();
        } catch (e) { console.error("Gagal load notif", e); }
    };

    const updateNotifBadge = () => {
        const headerBadge = document.getElementById('headerMenuBadge');
        const dropBadge = document.getElementById('notifBadgeCounter');
        const countText = document.getElementById('notifCountText');

        if (notifications.length > 0) {
            if(headerBadge) headerBadge.classList.remove('hidden');
            if(dropBadge) {
                dropBadge.innerText = notifications.length;
                dropBadge.classList.remove('hidden');
                dropBadge.classList.add('inline-flex', 'items-center', 'justify-center');
            }
            if(countText) countText.innerText = notifications.length;
        } else {
            if(headerBadge) headerBadge.classList.add('hidden');
            if(dropBadge) {
                dropBadge.classList.add('hidden');
                dropBadge.classList.remove('inline-flex', 'items-center', 'justify-center');
            }
            if(countText) countText.innerText = '0';
        }
    }

    const renderNotifications = () => {
        const container = document.getElementById('notifListContainer');
        if (!container) return;

        if (notifications.length === 0) {
            container.innerHTML = `<div class="text-center py-10 flex flex-col items-center justify-center opacity-50"><i class="fa-regular fa-bell-slash text-5xl text-gray-300 mb-3"></i><p class="text-xs text-gray-500 font-bold">Belum ada notifikasi baru.</p></div>`;
            return;
        }

        const grouped = {};
        notifications.forEach(notif => {
            const cat = getTimeCategory(notif.created_at);
            if (!grouped[cat]) grouped[cat] = [];
            grouped[cat].push(notif);
        });

        const catOrder = ["Pagi Ini", "Siang Ini", "Sore Ini", "Malam Ini", "Hari Ini", "Kemarin", "Minggu Ini", "Beberapa Minggu Lalu"];

        container.innerHTML = catOrder.map(cat => {
            if (!grouped[cat] || grouped[cat].length === 0) return '';

            const listHtml = grouped[cat].map(notif => {
                const timeStr = new Date(notif.created_at).toLocaleTimeString('id-ID', { hour: '2-digit', minute: '2-digit' });
                const isExam = notif.notif_type === 'exam';
                const theme = isExam
                    ? {
                        wrap: 'bg-blue-50/70 border-blue-100 hover:bg-blue-100/60',
                        iconWrap: 'text-blue-500 border-blue-50',
                        textMain: 'text-blue-700',
                        textAccent: 'text-blue-600',
                        icon: 'fa-file-pen',
                        label: 'UJIAN'
                    }
                    : {
                        wrap: 'bg-green-50/70 border-green-100 hover:bg-green-100/60',
                        iconWrap: 'text-green-500 border-green-50',
                        textMain: 'text-green-700',
                        textAccent: 'text-green-600',
                        icon: 'fa-book-open',
                        label: 'PR'
                    };

                const titleText = isExam
                    ? (notif.title || 'Ujian')
                    : `PR ${notif.subject}${notif.week ? ` Pertemuan ${notif.week}` : ''}`;

                return `
                <div onclick="openNotifDetail('${notif.id}')" class="${theme.wrap} border p-3.5 rounded-2xl flex gap-3 relative group cursor-pointer transition-colors">
                    <div class="w-8 h-8 rounded-full bg-white shadow-sm flex items-center justify-center ${theme.iconWrap} shrink-0 font-black text-xs border">
                        <i class="fa-solid ${theme.icon}"></i>
                    </div>

                    <div class="flex-1 min-w-0 pr-8 pointer-events-none">
                        <div class="flex items-center gap-1.5 mb-1">
                            <span class="text-[8px] font-black px-2 py-0.5 rounded-full ${isExam ? 'bg-blue-100 text-blue-600' : 'bg-green-100 text-green-600'}">
                                ${theme.label}
                            </span>
                            <span class="text-[8px] font-black text-gray-400 uppercase">${notif.subject || '-'}</span>
                        </div>

                        <p class="text-xs text-gray-700 leading-relaxed">
                            <b class="${theme.textMain}">${notif.student_name}</b>
                            telah mengerjakan <b class="${theme.textAccent}">${titleText}</b>.
                        </p>

                        <p class="text-[9px] font-bold text-gray-400 mt-1.5 uppercase tracking-wider">
                            <i class="fa-regular fa-clock"></i> ${timeStr}
                        </p>
                    </div>

                    <button onclick="event.stopPropagation(); deleteNotif('${notif.id}')" class="absolute right-3 top-1/2 -translate-y-1/2 w-8 h-8 bg-white border border-red-200 text-red-500 hover:text-red-700 hover:bg-red-50 rounded-xl flex items-center justify-center transition-all opacity-100 sm:opacity-0 sm:group-hover:opacity-100 active:scale-95 shadow-sm z-10">
                        <i class="fa-solid fa-trash-can"></i>
                    </button>
                </div>
                `;
            }).join('');

            return `
                <div class="mb-4">
                    <h4 class="text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-2 pl-1 border-b border-gray-100 pb-1">${cat}</h4>
                    <div class="space-y-2">
                        ${listHtml}
                    </div>
                </div>
            `;
        }).join('');
    };

    let cachedExamOptionsForLB = {};

    window.handleNotifLeaderboardModeChange = async () => {
        const mode = document.getElementById('lbMode')?.value || 'pr';
        const prFilters = document.getElementById('lbPRFilters');
        const examFilters = document.getElementById('lbExamFilters');

        if (mode === 'exam') {
            prFilters?.classList.add('hidden');
            examFilters?.classList.remove('hidden');
            await loadExamOptionsForLB();
        } else {
            examFilters?.classList.add('hidden');
            prFilters?.classList.remove('hidden');
            loadPRLeaderboard();
        }
    };

    window.loadExamOptionsForLB = async () => {
        const subject = document.getElementById('lbExamSubject')?.value || 'tajwid';
        const select = document.getElementById('lbExamId');

        if (!select) return;

        select.innerHTML = `<option value="">Memuat ujian...</option>`;

        try {
            if (!cachedExamOptionsForLB[subject]) {
                const res = await fetch(`${API_URL}/api/exams`, {
                    headers: { 'Authorization': `Bearer ${token}` }
                });

                const result = await res.json();

                const exams = (result.data || [])
                    .filter(e =>
                        e.is_active === true &&
                        String(e.subject || '').toLowerCase() === String(subject).toLowerCase()
                    );

                cachedExamOptionsForLB[subject] = exams;
            }

            const exams = cachedExamOptionsForLB[subject];

            if (exams.length === 0) {
                select.innerHTML = `<option value="">Belum ada ujian aktif</option>`;
                document.getElementById('lbPRContainer').innerHTML = `
                    <p class="text-center text-xs text-gray-400 italic py-10 font-medium">
                        Belum ada ujian aktif untuk mapel ini.
                    </p>
                `;
                return;
            }

            select.innerHTML = exams.map(e => `
                <option value="${e.id}">${e.title}</option>
            `).join('');

            loadPRLeaderboard();

        } catch (err) {
            console.error(err);
            select.innerHTML = `<option value="">Gagal memuat ujian</option>`;
        }
    };


    window.loadPRLeaderboard = async () => {
        const mode = document.getElementById('lbMode')?.value || 'pr';
        const container = document.getElementById('lbPRContainer');

        container.innerHTML = `
            <div class="text-center py-10">
                <i class="fa-solid fa-spinner fa-spin text-indigo-500 text-2xl"></i>
                <p class="text-[10px] text-indigo-400 font-bold mt-2">Menghitung Peringkat...</p>
            </div>
        `;

        try {
            let url = '';

            if (mode === 'exam') {
                const examId = document.getElementById('lbExamId')?.value;

                if (!examId) {
                    container.innerHTML = `<p class="text-center text-xs text-gray-400 italic py-10 font-medium">Pilih ujian terlebih dahulu.</p>`;
                    return;
                }

                url = `${API_URL}/api/onboarding/leaderboard-pr?mode=exam&exam_id=${examId}`;
            } else {
                const subj = document.getElementById('lbFilterSubject').value;
                const week = document.getElementById('lbFilterWeek').value;

                if (!subj || !week) return;

                url = `${API_URL}/api/onboarding/leaderboard-pr?subject=${subj}&week=${week}`;
            }

            const res = await fetch(url, {
                headers: { 'Authorization': `Bearer ${token}` }
            });

            const result = await res.json();

            let data = result.data || [];

            // PR hanya yang is_pr true
            if (mode === 'pr') {
                data = data.filter(d => d.is_pr === true);
            }

            // Exclude akun dummy/testing
            data = data.filter(d => {
                const name = String(d.name || '').toLowerCase();
                return !name.includes('john doe') && !name.includes('xaviera') && !name.includes('xaveria');
            });

            data = data.slice(0, 5);

            if (data.length === 0) {
                container.innerHTML = `
                    <p class="text-center text-xs text-gray-400 italic py-10 font-medium">
                        Belum ada data leaderboard ${mode === 'exam' ? 'ujian ini' : 'PR ini'}.
                    </p>
                `;
                return;
            }

            container.innerHTML = data.map((d, i) => {
                const rankColors = [
                    'bg-amber-100 text-amber-600 border-amber-200',
                    'bg-slate-100 text-slate-500 border-slate-200',
                    'bg-orange-100 text-orange-600 border-orange-200',
                    'bg-indigo-50 text-indigo-500 border-indigo-100',
                    'bg-indigo-50 text-indigo-500 border-indigo-100'
                ];

                const modeBadge = mode === 'exam'
                    ? `<span class="bg-blue-50 text-blue-600 border border-blue-100 text-[8px] font-black px-2 py-0.5 rounded-lg">UJIAN</span>`
                    : `<span class="bg-green-50 text-green-600 border border-green-100 text-[8px] font-black px-2 py-0.5 rounded-lg">PR</span>`;

                return `
                    <div class="flex items-center gap-3 bg-white border border-gray-100 rounded-2xl p-3 shadow-sm">
                        <div class="w-9 h-9 rounded-xl flex items-center justify-center font-black text-xs border ${rankColors[i] || rankColors[4]}">
                            #${i + 1}
                        </div>

                        <div class="flex-1 min-w-0">
                            <div class="flex items-center gap-1.5 mb-1">
                                ${modeBadge}
                                <p class="font-black text-xs text-gray-800 truncate">${d.name}</p>
                            </div>
                            <p class="text-[9px] font-bold text-gray-400">
                                Nilai asli: ${d.raw_score ?? d.score ?? 0}
                                ${d.time_taken && d.time_taken !== '-' ? ` • ${d.time_taken}s` : ''}
                            </p>
                        </div>

                        <div class="text-right">
                            <p class="text-lg font-black text-indigo-600">${d.composite_score}</p>
                            <p class="text-[8px] font-bold text-gray-400 uppercase">Poin</p>
                        </div>
                    </div>
                `;
            }).join('');

        } catch (err) {
            console.error(err);
            container.innerHTML = `
                <div class="text-center py-10 bg-red-50 rounded-2xl border border-red-100">
                    <i class="fa-solid fa-triangle-exclamation text-red-500 text-2xl mb-2"></i>
                    <p class="text-xs font-bold text-red-500">Gagal memuat leaderboard.</p>
                </div>
            `;
        }
    };


    window.openNotifModal = () => {
        const modal = document.getElementById('modalNotif');
        const box = document.getElementById('notifBox');

        switchNotifTab('list');

        modal.classList.replace('hidden', 'flex');
        setTimeout(() => {
            modal.classList.replace('opacity-0', 'opacity-100');
            box.classList.replace('translate-y-full', 'translate-y-0');
        }, 10);
        loadNotifications(false);
    };

    window.closeNotifModal = () => {
        const modal = document.getElementById('modalNotif');
        const box = document.getElementById('notifBox');
        modal.classList.replace('opacity-100', 'opacity-0');
        if (window.innerWidth < 640) box.classList.replace('translate-y-0', 'translate-y-full');
        setTimeout(() => modal.classList.replace('flex', 'hidden'), 300);
    };

    window.deleteNotif = (id) => {
        showConfirmGlobal('Hapus Notifikasi?', 'Notif ini akan dihapus permanen dari laci.', async () => {
            notifications = notifications.filter(n => n.id !== id);
            updateNotifBadge();
            renderNotifications();
            try { await fetch(`${API_URL}/api/notifications/${id}`, { method: 'DELETE', headers: { 'Authorization': `Bearer ${token}` }}); } catch (e) {}
        }, false, 'danger');
    };

    window.clearAllNotifs = () => {
        if (notifications.length === 0) return;
        showConfirmGlobal('Hapus Semua?', 'Yakin mau hapus semua notifikasi? Laci akan kembali kosong.', async () => {
            notifications = [];
            updateNotifBadge();
            renderNotifications();
            try { await fetch(`${API_URL}/api/notifications/clear`, { method: 'DELETE', headers: { 'Authorization': `Bearer ${token}` }}); } catch (e) {}
        }, false, 'danger');
    };

    window.openNotifDetail = async (notifId) => {
        const modal = document.getElementById('modalNotifDetail');
        const box = document.getElementById('notifDetailBox');
        const loading = document.getElementById('notifDetailLoading');
        const content = document.getElementById('notifDetailContent');

        modal.classList.replace('hidden', 'flex');
        setTimeout(() => {
            modal.classList.replace('opacity-0', 'opacity-100');
            box.classList.replace('scale-95', 'scale-100');
        }, 10);

        loading.classList.remove('hidden');
        content.classList.add('hidden');

        try {
            const res = await fetch(`${API_URL}/api/notifications/${notifId}/detail`, {
                headers: {
                    'Authorization': `Bearer ${token}`
                }
            });

            const result = await res.json();

            if (!res.ok || result.status === 'error') {
                throw new Error(result.message || 'Gagal mengambil detail notifikasi.');
            }

            const detail = result.data;
            const isExam = detail.type === 'exam';

            document.getElementById('detailStudentName').innerText = detail.student_name || 'Siswa';

            document.getElementById('detailSubjectWeek').innerText = isExam
                ? `${detail.subject || 'UJIAN'} • ${detail.title || 'Ujian'}`
                : `${detail.subject || 'PR'} • PERTEMUAN ${detail.week || '-'}`;

            const scoreEl = document.getElementById('detailScore');
            const score = Number(detail.score || 0);

            scoreEl.innerText = score;
            scoreEl.className = `text-5xl font-black drop-shadow-sm ${
                score >= 80 ? 'text-green-600' : (score >= 60 ? 'text-amber-500' : 'text-red-500')
            }`;

            const photoEl = document.getElementById('detailSatpamPhoto');
            const placeholderEl = document.getElementById('detailSatpamPlaceholder');

            if (detail.photo_url) {
                photoEl.src = detail.photo_url;
                photoEl.classList.remove('hidden');
                placeholderEl.classList.add('hidden');
            } else {
                photoEl.src = '';
                photoEl.classList.add('hidden');
                placeholderEl.classList.remove('hidden');
            }

            loading.classList.add('hidden');
            content.classList.remove('hidden');

        } catch (err) {
            loading.innerHTML = `
                <div class="text-center">
                    <i class="fa-solid fa-triangle-exclamation text-red-500 text-3xl mb-3"></i>
                    <p class="text-xs font-bold text-gray-500">${err.message}</p>
                </div>
            `;
        }
    };

    window.closeNotifDetail = () => {
        const modal = document.getElementById('modalNotifDetail');
        const box = document.getElementById('notifDetailBox');

        modal.classList.replace('opacity-100', 'opacity-0');
        box.classList.replace('scale-100', 'scale-95');
        setTimeout(() => modal.classList.replace('flex', 'hidden'), 300);
    };


    // ==============================================================
    // LOGIKA PENGATURAN LANJUTAN & RESET TAHUN AJARAN & MAINTENANCE
    // ==============================================================
    window.toggleDangerZone = () => {
        const content = document.getElementById('dangerContent');
        const icon = document.getElementById('dangerIcon');
        if(content) content.classList.toggle('hidden');
        if(icon) icon.classList.toggle('rotate-180');
    };

    // [BARU] Logika Toggle Maintenance
    window.promptMaintenanceToggle = (checkbox) => {
        const newState = checkbox.checked;
        checkbox.checked = !newState; // Revert sementara sampai dikonfirmasi

        const actionText = newState ? 'Mengaktifkan' : 'Mematikan';
        const msgText = newState
            ? 'Semua akses murid ke halaman PR akan diblokir dan dialihkan ke halaman Maintenance.'
            : 'Murid dapat kembali mengakses halaman PR secara normal.';

        showConfirmGlobal(
            `${actionText} Maintenance?`,
            msgText,
            () => executeMaintenanceToggle(newState, checkbox),
            false,
            'warning'
        );
    };

    window.executeMaintenanceToggle = async (newState, checkbox) => {
        try {
            const res = await fetch(`${API_URL}/api/onboarding/system/status`, {
                method: 'POST',
                headers: { 'Authorization': `Bearer ${token}`, 'Content-Type': 'application/json' },
                body: JSON.stringify({ is_maintenance: newState })
            });

            const result = await res.json();
            if (!res.ok) throw new Error(result.message || 'Gagal update status');

            checkbox.checked = newState;
            window.showToast?.('success', 'Berhasil', `Mode perbaikan berhasil di${newState ? 'aktifkan' : 'matikan'}.`);
        } catch (e) {
            window.showToast?.('danger', 'Error', e.message);
        }
    };


    const resetPassInput = document.getElementById('resetPasswordInput');
    const btnExecuteReset = document.getElementById('btnExecuteReset');

    window.openResetModal = () => {
        if (resetPassInput) resetPassInput.value = '';
        if (btnExecuteReset) { btnExecuteReset.disabled = true; btnExecuteReset.innerHTML = 'Hapus Data'; }
        document.getElementById('modalReset')?.classList.replace('hidden', 'flex');
        setTimeout(() => { if (resetPassInput) resetPassInput.focus() }, 100);
    };

    window.closeResetModal = () => { document.getElementById('modalReset')?.classList.replace('flex', 'hidden'); };

    if (resetPassInput && btnExecuteReset) {
        resetPassInput.addEventListener('input', (e) => { btnExecuteReset.disabled = e.target.value.trim().length < 3; });

        btnExecuteReset.onclick = async () => {
            const password = resetPassInput.value.trim();
            if (!password) return;
            if (!confirm('Peringatan Terakhir: Anda yakin ingin menghapus SELURUH data transaksi?')) return;

            btnExecuteReset.disabled = true;
            btnExecuteReset.innerHTML = '<i class="fa-solid fa-spinner fa-spin"></i> Memproses...';

            try {
                const res = await fetch(`${API_URL}/api/admin/factory-reset`, {
                    method: 'POST',
                    headers: { 'Authorization': `Bearer ${token}`, 'Content-Type': 'application/json' },
                    body: JSON.stringify({ password: password })
                });
                const result = await res.json();

                if (res.ok) {
                    const blob = new Blob([JSON.stringify(result.backup, null, 2)], { type: "application/json" });
                    const url = URL.createObjectURL(blob);
                    const a = document.createElement('a');
                    a.href = url; a.download = `Backup_YukNgaji_${new Date().toISOString().split('T')[0]}.json`;
                    document.body.appendChild(a); a.click(); document.body.removeChild(a); URL.revokeObjectURL(url);

                    window.showToast?.('success', 'Reset Selesai', 'Data dihapus dan dibackup.');
                    closeResetModal(); setTimeout(() => window.location.reload(), 2000);
                } else throw new Error(result.message);
            } catch (err) {
                window.showToast?.('danger', 'Akses Ditolak', err.message || 'Terjadi kesalahan.');
                btnExecuteReset.innerHTML = 'Hapus Data'; btnExecuteReset.disabled = true; resetPassInput.value = ''; resetPassInput.focus();
            }
        };
    }

    // ==============================================================
    // LOGIKA TUNGGAKAN TUGAS (GLOBAL & MODAL)
    // ==============================================================
    window.prLocksCache = {};
    const initFiltersAndTasks = async () => {
        try {
            const headers = { 'Authorization': `Bearer ${token}` };
            const res = await fetch(`${API_URL}/api/insights/filters`, { headers });

            if(res.ok) {
                const filterData = await res.json();
                availableTasks = filterData.data.available_tasks || [];
                const subjectsList = filterData.data.subjects || [];

                const locksPromises = subjectsList.map(s =>
                    fetch(`${API_URL}/api/onboarding/pr-locks/${s.toLowerCase()}`, { headers })
                    .then(r => r.json())
                    .catch(() => ({data: []}))
                );
                const locksResults = await Promise.all(locksPromises);

                subjectsList.forEach((s, i) => {
                    window.prLocksCache[s.toLowerCase()] = locksResults[i].data || [];
                });

                const subjSelect = document.getElementById('modalFilterSubject');
                const weekSelect = document.getElementById('modalFilterWeek');

                if (subjSelect) {
                    subjSelect.innerHTML = `<option value="">Semua Pelajaran</option>` +
                        subjectsList.map(s => `<option value="${s.toLowerCase()}">${s}</option>`).join('');
                }

                const lbWeekSelect = document.getElementById('lbFilterWeek');
                const weeksHtml = (filterData.data.weeks || []).map(w => `<option value="${w}">Pert. ${w}</option>`).join('');

                if (weekSelect) {
                    weekSelect.innerHTML = `<option value="">Semua Pertemuan</option>` + weeksHtml;
                }

                if (lbWeekSelect) {
                    lbWeekSelect.innerHTML = weeksHtml;
                }

                subjSelect?.addEventListener('change', renderNotDoneModalList);
                weekSelect?.addEventListener('change', renderNotDoneModalList);
            }

            calculateGlobalMissingTasks();
        } catch (e) { console.error("Gagal memuat filter dan task", e); }
    };

    const calculateGlobalMissingTasks = () => {
        enrichedStudents.forEach(student => {
            const completed = student.completed_tasks || [];
            student.missing_tasks = availableTasks.filter(available =>
                !completed.some(done => done.subject === available.subject && done.week === available.week)
            );
        });

        const laggingStudents = enrichedStudents.filter(s => s.missing_tasks.length > 0);
        const notDoneCountEl = document.getElementById('notDoneCount');
        if (notDoneCountEl) notDoneCountEl.innerText = laggingStudents.length;

        refreshMissingSummaryCount();
    };

    const refreshMissingSummaryCount = async () => {
        try {
            const prLaggingIds = new Set(
                enrichedStudents
                    .filter(s => (s.missing_tasks || []).length > 0)
                    .map(s => String(s.id))
            );

            const res = await fetch(`${API_URL}/api/insights/exam-missing`, {
                headers: {
                    'Authorization': `Bearer ${token}`
                }
            });

            const result = await res.json();
            const examLaggingIds = new Set(
                ((result.data?.students) || []).map(s => String(s.id))
            );

            const merged = new Set([...prLaggingIds, ...examLaggingIds]);

            const notDoneCountEl = document.getElementById('notDoneCount');
            if (notDoneCountEl) notDoneCountEl.innerText = merged.size;

        } catch (e) {
            console.error('Gagal menghitung ringkasan tunggakan gabungan:', e);
        }
    };

    const renderNotDoneModalList = () => {
        const subjFilter = document.getElementById('modalFilterSubject')?.value;
        const weekFilter = parseInt(document.getElementById('modalFilterWeek')?.value);
        const container = document.getElementById('notDoneListContainer');

        if (!container) return;

        const filteredStudents = enrichedStudents.filter(student => {
            if (!student.missing_tasks || student.missing_tasks.length === 0) return false;
            return student.missing_tasks.some(mt => {
                const matchSubj = subjFilter ? mt.subject.toLowerCase() === subjFilter : true;
                const matchWeek = weekFilter ? mt.week === weekFilter : true;
                return matchSubj && matchWeek;
            });
        });

        if (filteredStudents.length === 0) {
            container.innerHTML = `<p class="text-center py-10 text-xs text-gray-400 italic">Alhamdulillah, tidak ada tunggakan sesuai kriteria ini! 🎉</p>`;
            return;
        }

        container.innerHTML = filteredStudents.map(student => {
            const relevantMissing = student.missing_tasks.filter(mt => {
                const matchSubj = subjFilter ? mt.subject.toLowerCase() === subjFilter : true;
                const matchWeek = weekFilter ? mt.week === weekFilter : true;
                return matchSubj && matchWeek;
            });

            let badgesHtml = '';
            let wrapperClass = 'items-center';

            if (!subjFilter || !weekFilter) {
                const badges = relevantMissing.slice(0, 2).map(mt => {
                    const isPR = window.prLocksCache[mt.subject.toLowerCase()]?.includes(mt.week);
                    const prLabel = isPR
                        ? `<span class="bg-indigo-500 text-white px-1 py-0.5 rounded-[4px] ml-1 flex items-center gap-0.5 shadow-sm"><i class="fa-solid fa-house-laptop text-[6px]"></i> PR</span>`
                        : '';

                    return `<span class="flex items-center text-[8px] bg-red-100 text-red-600 px-1.5 py-0.5 rounded font-black uppercase tracking-widest border border-red-200">
                        ${mt.subject} (Pert ${mt.week})${prLabel}
                    </span>`;
                }).join('');

                const moreCount = relevantMissing.length > 2 ? `<span class="flex items-center text-[8px] bg-gray-100 text-gray-500 px-1.5 py-0.5 rounded font-bold border border-gray-200">+${relevantMissing.length - 2}</span>` : '';

                badgesHtml = `<div class="flex flex-wrap gap-1 mt-1.5">${badges}${moreCount}</div>`;
                wrapperClass = 'items-start pt-0.5';
            }

            return `
                <div class="flex items-center justify-between p-3 rounded-2xl bg-gray-50 border border-gray-100 hover:border-red-200 hover:bg-red-50 transition-all cursor-pointer group" onclick="window.location.href='/student/${student.id}'">
                    <div class="flex ${wrapperClass} gap-3">
                        <div class="w-10 h-10 rounded-xl bg-white flex items-center justify-center font-bold text-xs text-gray-400 border border-gray-200 group-hover:border-red-200 overflow-hidden shadow-sm shrink-0">
                            ${student.photo_url ? `<img src="${student.photo_url}" class="w-full h-full object-cover">` : student.name.substring(0,2).toUpperCase()}
                        </div>
                        <div>
                            <span class="text-xs font-bold text-gray-700 group-hover:text-red-600 block leading-tight">${student.name}</span>
                            ${badgesHtml}
                        </div>
                    </div>
                    <i class="fa-solid fa-arrow-right text-[10px] text-gray-300 group-hover:text-red-400"></i>
                </div>
            `;
        }).join('');
    };

    window.openNotDoneModal = () => {
        const modal = document.getElementById('modalNotDone');
        if (modal) modal.classList.replace('hidden', 'flex');

        window.switchMissingTab(missingTab || 'pr');
    };

    window.closeNotDoneModal = () => {
        const modal = document.getElementById('modalNotDone');
        if (modal) modal.classList.replace('flex', 'hidden');
    };

    window.switchMissingTab = (tab) => {
        missingTab = tab;

        const tabPR = document.getElementById('tabMissingPR');
        const tabExam = document.getElementById('tabMissingExam');
        const prFilters = document.getElementById('missingPRFilters');
        const examFilters = document.getElementById('missingExamFilters');

        const activePRClass = "flex-1 py-2.5 text-[10px] font-bold bg-white text-red-600 rounded-xl shadow-sm transition-all uppercase tracking-widest";
        const inactiveClass = "flex-1 py-2.5 text-[10px] font-bold text-gray-400 hover:text-gray-600 transition-all uppercase tracking-widest";

        if (tab === 'pr') {
            tabPR.className = activePRClass;
            tabExam.className = inactiveClass;

            prFilters.classList.remove('hidden');
            prFilters.classList.add('flex');

            examFilters.classList.add('hidden');

            renderNotDoneModalList();
            return;
        }

        tabExam.className = activePRClass;
        tabPR.className = inactiveClass;

        prFilters.classList.add('hidden');
        prFilters.classList.remove('flex');

        examFilters.classList.remove('hidden');

        loadMissingExamList();
    };

    window.loadMissingExamList = async () => {
        const container = document.getElementById('notDoneListContainer');
        const subject = document.getElementById('modalFilterExamSubject')?.value || '';
        const cacheKey = subject || 'all';

        if (!container) return;

        container.innerHTML = `
            <div class="text-center py-8">
                <i class="fa-solid fa-spinner fa-spin text-red-500 text-xl"></i>
                <p class="text-[10px] font-bold text-gray-400 mt-2">Memuat tunggakan ujian...</p>
            </div>
        `;

        try {
            if (!missingExamCache[cacheKey]) {
                const query = subject ? `?subject=${encodeURIComponent(subject)}` : '';
                const res = await fetch(`${API_URL}/api/insights/exam-missing${query}`, {
                    headers: {
                        'Authorization': `Bearer ${token}`
                    }
                });

                const result = await res.json();

                if (!res.ok || result.status === 'error') {
                    throw new Error(result.message || 'Gagal memuat data ujian.');
                }

                missingExamCache[cacheKey] = result.data;
            }

            renderMissingExamList(missingExamCache[cacheKey]);

        } catch (err) {
            console.error(err);
            container.innerHTML = `
                <div class="text-center py-8 bg-red-50 border border-red-100 rounded-2xl">
                    <i class="fa-solid fa-triangle-exclamation text-red-500 text-2xl mb-2"></i>
                    <p class="text-xs font-bold text-red-500">Gagal memuat data tunggakan ujian.</p>
                </div>
            `;
        }
    };

    const renderMissingExamList = (data) => {
        const container = document.getElementById('notDoneListContainer');
        if (!container) return;

        const students = data?.students || [];
        const subject = document.getElementById('modalFilterExamSubject')?.value || '';

        if (students.length === 0) {
            container.innerHTML = `
                <div class="text-center py-10 bg-green-50 rounded-3xl border border-green-100">
                    <i class="fa-solid fa-circle-check text-green-500 text-3xl mb-3"></i>
                    <p class="text-xs font-black text-green-600">Semua siswa sudah mengerjakan ujian${subject ? ' mapel ini' : ''}.</p>
                </div>
            `;
            return;
        }

        container.innerHTML = students.map(student => {
            const initials = (student.name || '??').substring(0, 2).toUpperCase();

            const badgesHtml = (student.missing_exams || []).map(exam => `
                <span class="inline-block mt-1 mr-1 bg-indigo-50 text-indigo-600 border border-indigo-100 px-2 py-0.5 rounded-lg text-[8px] font-black uppercase tracking-wider">
                    ${exam.subject} • ${exam.title}
                </span>
            `).join('');

            return `
                <div class="bg-white border border-red-100 rounded-2xl p-3 shadow-sm hover:bg-red-50 transition-colors">
                    <div class="flex items-start justify-between gap-3">
                        <div class="flex items-start gap-3 min-w-0">
                            <div class="w-10 h-10 bg-red-50 text-red-500 rounded-xl flex items-center justify-center font-black text-xs shrink-0">
                                ${initials}
                            </div>
                            <div class="min-w-0">
                                <span class="text-xs font-black text-gray-800 block leading-tight capitalize">${student.name}</span>
                                <p class="text-[9px] font-bold text-red-400 mt-1">${student.missing_count} ujian belum dikerjakan</p>
                                <div class="mt-1">
                                    ${badgesHtml}
                                </div>
                            </div>
                        </div>
                        <i class="fa-solid fa-file-circle-xmark text-red-300 text-sm mt-1"></i>
                    </div>
                </div>
            `;
        }).join('');
    };

    // ==============================================================
    // FETCH DATA UTAMA
    // ==============================================================
    const fetchDashboardData = async () => {
        try {
            const headers = { 'Authorization': `Bearer ${token}`, 'Content-Type': 'application/json' };

            // [UPDATE] Sekalian fetch status maintenance dari backend saat load dashboard
            try {
                const sysRes = await fetch(`${API_URL}/api/onboarding/system/status`, { headers });
                if (sysRes.ok) {
                    const sysData = await sysRes.json();
                    if (sysData.data) {
                        const toggleBtn = document.getElementById('toggleMaintenance');
                        if (toggleBtn) toggleBtn.checked = sysData.data.is_maintenance;
                    }
                }
            } catch (e) { console.log('Gagal fetch system status'); }

            const [dashRes, enrichedRes] = await Promise.all([
                fetch(`${API_URL}/api/insights/dashboard`, { headers }),
                fetch(`${API_URL}/api/students/stats/enriched`, { headers })
            ]);

            if (dashRes.status === 401 || enrichedRes.status === 401) throw new Error('Sesi habis');

            const dashData = await dashRes.json();
            const enrichedData = await enrichedRes.json();

            enrichedStudents = enrichedData.data || [];
            missingExamCache = {};

            const statStudentsEl = document.getElementById('statStudents');
            if (statStudentsEl) statStudentsEl.innerText = dashData.data.total_students || enrichedStudents.length || 0;

            const statExamsEl = document.getElementById('statExams');
            if (statExamsEl) statExamsEl.innerText = dashData.data.total_exams || 0;

            renderStudentList();
            renderLeaderboard();
            await initFiltersAndTasks();

            loadNotifications(true);

        } catch (error) {
            if (window.showToast) window.showToast('danger', 'Error', 'Gagal memuat data dashboard.');
            document.getElementById('studentList').innerHTML = '<div class="text-center py-6 text-red-500 text-xs font-bold">Gagal memuat.</div>';
            document.getElementById('podiumContainer').innerHTML = '<div class="text-center w-full text-red-500 text-xs font-bold">Gagal memuat.</div>';
        }
    };

    const renderStudentList = () => {
        const filterEl = document.getElementById('filterList');
        if (!filterEl) return;

        const filter = filterEl.value;
        const container = document.getElementById('studentList');
        const remContainer = document.getElementById('remainingStudentsContainer');
        const remText = document.getElementById('remainingStudentsText');

        if (enrichedStudents.length === 0) {
            container.innerHTML = `<div class="text-center py-10 bg-white rounded-3xl border border-gray-100 border-dashed"><i class="fa-solid fa-folder-open text-gray-200 text-5xl mb-3 block"></i><p class="text-gray-400 text-xs font-bold">Belum ada data Anak.</p></div>`;
            if (remContainer) remContainer.classList.add('hidden');
            return;
        }

        let sorted = [...enrichedStudents];
        if (filter === 'abjad') sorted.sort((a,b) => a.name.localeCompare(b.name));
        else if (filter === 'nilai') sorted.sort((a,b) => b.finalScore - a.finalScore);
        else if (filter === 'rajin') sorted.sort((a,b) => b.hadir - a.hadir);

        const top5 = sorted.slice(0, 5);
        container.innerHTML = top5.map((student, idx) => {
            const initials = student.name.substring(0, 2).toUpperCase();
            const avatarHtml = student.photo_url ? `<img src="${student.photo_url}" class="w-full h-full object-cover" alt="${student.name}" />` : initials;

            let detailBadge = '';
            if (filter === 'nilai') detailBadge = `<span class="bg-amber-100 text-amber-600 px-2 py-0.5 rounded text-[9px] font-bold ml-2"><i class="fa-solid fa-star text-[8px]"></i> Rata: ${student.finalScore}</span>`;
            else if (filter === 'rajin') detailBadge = `<span class="bg-blue-100 text-blue-600 px-2 py-0.5 rounded text-[9px] font-bold ml-2"><i class="fa-solid fa-calendar-check text-[8px]"></i> Hadir: ${student.hadir}x</span>`;

            return `
                <div class="bg-white p-4 rounded-2xl border border-gray-50 shadow-sm flex items-center justify-between active:scale-[0.98] transition-all cursor-pointer hover:border-green-200" onclick="window.location.href='/student/${student.id}'">
                    <div class="flex items-center gap-3">
                        <div class="relative">
                            <div class="w-12 h-12 bg-green-50 text-green-600 font-bold rounded-2xl flex items-center justify-center text-lg shadow-inner overflow-hidden">
                                ${avatarHtml}
                            </div>
                            ${filter !== 'abjad' ? `<div class="absolute -top-1 -left-1 w-5 h-5 bg-gray-800 text-white rounded-full flex items-center justify-center text-[9px] font-black border-2 border-white shadow-sm z-10">${idx + 1}</div>` : ''}
                        </div>
                        <div>
                            <h4 class="font-bold text-gray-800 text-sm truncate max-w-[150px]">${student.name}</h4>
                            <div class="flex items-center mt-0.5">
                                <p class="text-[10px] text-gray-400 font-bold uppercase tracking-wider">${student.grade}</p>
                                ${detailBadge}
                            </div>
                        </div>
                    </div>
                    <div class="w-8 h-8 bg-gray-50 rounded-full flex items-center justify-center text-gray-300">
                        <i class="fa-solid fa-chevron-right text-[10px]"></i>
                    </div>
                </div>
            `;
        }).join('');

        if (remContainer && remText) {
            if (sorted.length > 5) {
                remText.innerText = `+ ${sorted.length - 5} anak lainnya`;
                remContainer.classList.remove('hidden');
            } else {
                remContainer.classList.add('hidden');
            }
        }
    };

    const filterListEl = document.getElementById('filterList');
    if (filterListEl) filterListEl.addEventListener('change', renderStudentList);

    const renderLeaderboard = () => {
        const podiumContainer = document.getElementById('podiumContainer');
        const tableBody = document.getElementById('leaderboardTable');

        if (!podiumContainer || !tableBody) return;

        if (enrichedStudents.length === 0) {
            podiumContainer.innerHTML = `<div class="text-center w-full"><p class="text-gray-400 text-xs font-bold">Belum ada data untuk Leaderboard.</p></div>`;
            return;
        }

        const ranked = [...enrichedStudents].sort((a,b) => b.finalScore - a.finalScore);
        const top5 = ranked.slice(0, 5);

        const generateAvatar = (student, colorClass) => {
            if(!student) return `<div class="w-12 h-12 rounded-full border-2 border-dashed border-gray-300 bg-gray-50"></div>`;

            const avatarHtml = student.photo_url
                ? `<img src="${student.photo_url}" class="w-full h-full object-cover rounded-full" alt="Foto ${student.name}" />`
                : student.name.substring(0,2).toUpperCase();

            return `
                <div class="relative w-14 h-14 ${colorClass} rounded-full flex items-center justify-center font-black text-lg z-10 shadow-lg border-2 border-white cursor-pointer hover:-translate-y-1 transition-transform overflow-hidden" onclick="window.location.href='/student/${student.id}'">
                    ${avatarHtml}
                </div>
                <p class="text-[9px] font-bold mt-1 text-gray-700 truncate w-16 text-center capitalize">${student.name.split(' ')[0]}</p>
                <p class="text-[8px] font-black text-amber-500"><i class="fa-solid fa-star"></i> ${student.finalScore}</p>
            `;
        }

        podiumContainer.innerHTML = `
            <div class="flex flex-col items-center">
                ${generateAvatar(top5[1], 'bg-slate-200 text-slate-600')}
                <div class="w-20 h-20 bg-gradient-to-t from-slate-300 to-slate-100 rounded-t-xl shadow-inner flex items-start justify-center pt-2 mt-2 border border-slate-200">
                    <span class="text-slate-400 font-black text-2xl drop-shadow-sm">2</span>
                </div>
            </div>

            <div class="flex flex-col items-center">
                <i class="fa-solid fa-crown text-amber-400 text-3xl mb-1 animate-bounce drop-shadow-md"></i>
                ${generateAvatar(top5[0], 'bg-amber-100 text-amber-600 border-4 border-amber-300 w-16 h-16 text-2xl')}
                <div class="w-24 h-28 bg-gradient-to-t from-amber-400 to-amber-200 rounded-t-xl shadow-inner flex items-start justify-center pt-2 mt-2 border border-amber-300">
                    <span class="text-amber-600 font-black text-4xl drop-shadow-sm">1</span>
                </div>
            </div>

            <div class="flex flex-col items-center">
                ${generateAvatar(top5[2], 'bg-orange-100 text-orange-600')}
                <div class="w-20 h-16 bg-gradient-to-t from-orange-300 to-orange-100 rounded-t-xl shadow-inner flex items-start justify-center pt-2 mt-2 border border-orange-200">
                    <span class="text-orange-400 font-black text-xl drop-shadow-sm">3</span>
                </div>
            </div>
        `;

        tableBody.innerHTML = top5.map((s, i) => {
            const miniAvatar = s.photo_url
                ? `<img src="${s.photo_url}" class="w-full h-full object-cover" />`
                : `<span class="text-[10px]">${s.name.substring(0,2).toUpperCase()}</span>`;

            return `
            <tr class="hover:bg-gray-50 transition-colors cursor-pointer" onclick="window.location.href='/student/${s.id}'">
                <td class="p-3">
                    <div class="flex items-center gap-2">
                        <div class="w-6 h-6 rounded-md bg-gray-100 flex items-center justify-center font-black text-gray-500 overflow-hidden shadow-inner flex-shrink-0">
                            ${miniAvatar}
                        </div>
                        <div>
                            <p class="font-bold text-gray-800 truncate max-w-[90px]">${s.name.split(' ')[0]}</p>
                            <p class="text-[8px] font-bold text-gray-400 uppercase">${s.grade}</p>
                        </div>
                    </div>
                </td>
                <td class="p-3 text-center font-bold text-green-600">${s.t_avg > 0 ? s.t_avg : '-'}</td>
                <td class="p-3 text-center font-bold text-blue-600">${s.f_avg > 0 ? s.f_avg : '-'}</td>
                <td class="p-3 text-center font-bold text-purple-600">${s.th_avg > 0 ? s.th_avg : '-'}</td>
                <td class="p-3 text-center font-black text-gray-700 bg-gray-50/50">${s.hadir}x</td>
            </tr>
            `;
        }).join('');
    };

    fetchDashboardData();

    document.getElementById('btnLogout')?.addEventListener('click', () => {
        localStorage.clear();
        window.location.replace('/');
    });

    // --- CEK ABSENSI ---
    const checkAbsensiStatus = async () => {
        try {
            const res = await fetch(`${API_URL}/api/attendances/today-status`, { headers: { 'Authorization': `Bearer ${token}` }});
            const result = await res.json();

            const statusText = document.getElementById('absensiStatusText');
            const pulseBox = document.getElementById('absensiPulse');

            if (!statusText || !pulseBox) return;

            if (result.isDone) {
                statusText.innerText = "Selesai (Sudah Absen)";
                statusText.classList.replace('text-gray-400', 'text-green-500');
            } else {
                statusText.innerText = result.isMandatory ? "Wajib Absen Sekarang!" : "Absensi Sunnah";
                if (result.isMandatory) {
                    statusText.classList.replace('text-gray-400', 'text-red-500');
                    pulseBox.classList.replace('border-transparent', 'border-red-500/50');
                    pulseBox.classList.add('animate-pulse');
                }
            }
        } catch (e) {}
    };
    checkAbsensiStatus();

    // ==============================================================
    // LOGIKA TRANSFER KOIN REWARD
    // ==============================================================
    window.openTransferDrawer = () => {
        const select = document.getElementById('transferStudentSelect');
        const amountInput = document.getElementById('transferAmountInput');

        if (enrichedStudents && enrichedStudents.length > 0) {
            select.innerHTML = '<option value="" disabled selected>-- Siapa yang berhasil jawab? --</option>' +
                enrichedStudents.map(s => `<option value="${s.id}">${s.name}</option>`).join('');
        } else {
            select.innerHTML = '<option value="">Gagal memuat siswa.</option>';
        }

        amountInput.value = '';
        document.getElementById('drawerTransfer').classList.replace('hidden', 'flex');
    };

    window.closeTransferDrawer = () => {
        document.getElementById('drawerTransfer').classList.replace('flex', 'hidden');
    };

    window.setTransferAmount = (val) => {
        document.getElementById('transferAmountInput').value = val;
    };

    window.submitTransfer = async () => {
        const studentId = document.getElementById('transferStudentSelect').value;
        const amount = parseInt(document.getElementById('transferAmountInput').value);

        if (!studentId || !amount || amount <= 0) {
            window.showToast?.('warning', 'Ups', 'Pilih murid dan pastikan koin diisi dengan benar.');
            return;
        }

        const btn = document.getElementById('btnSubmitTransfer');
        const originalText = btn.innerHTML;
        btn.innerHTML = '<i class="fa-solid fa-spinner fa-spin"></i> Mengirim...';
        btn.disabled = true;

        try {
            const res = await fetch(`${API_URL}/api/onboarding/transfer-reward`, {
                method: 'POST',
                headers: { 'Authorization': `Bearer ${token}`, 'Content-Type': 'application/json' },
                body: JSON.stringify({
                    student_id: studentId,
                    amount: amount
                })
            });

            if (!res.ok) throw new Error('Gagal mengirim koin');

            window.showToast?.('success', 'Terkirim!', `Berhasil transfer ${amount} koin sebagai reward.`);
            closeTransferDrawer();

            fetchDashboardData();

        } catch (err) {
            window.showToast?.('danger', 'Error', 'Gagal mentransfer koin. Coba lagi.');
        } finally {
            btn.innerHTML = originalText;
            btn.disabled = false;
        }
    };

    window.openDailyChallengeLinkModal = () => {
        document.getElementById('modalDailyChallengeLink')?.classList.replace('hidden', 'flex');
    };

    window.closeDailyChallengeLinkModal = () => {
        document.getElementById('modalDailyChallengeLink')?.classList.replace('flex', 'hidden');
    };

    window.copyDailyChallengeLink = async (mode) => {
        const origin = window.location.origin;

        const link = mode === 'preview'
            ? `${origin}/daily-challenge?preview=true&token=${token}`
            : `${origin}/daily-challenge?token=${token}`;

        const text = mode === 'preview'
            ? `Link simulasi Daily Challenge:\n${link}`
            : `Assalamualaikum, ini link Daily Challenge hari ini untuk mendapatkan koin:\n${link}`;

        await navigator.clipboard.writeText(text);

        window.showToast?.(
            'success',
            'Link Disalin',
            mode === 'preview'
                ? 'Link simulasi sudah disalin.'
                : 'Link real murid sudah disalin.'
        );

        window.closeDailyChallengeLinkModal();
    };
</script>

<style>
    .no-scrollbar::-webkit-scrollbar { display: none; }
    .animate-slide-up { animation: slideUp 0.4s cubic-bezier(0.16, 1, 0.3, 1) forwards; }

    .custom-scrollbar::-webkit-scrollbar { width: 4px; }
    .custom-scrollbar::-webkit-scrollbar-thumb { background: #cbd5e1; border-radius: 10px; }
    .custom-scrollbar::-webkit-scrollbar-track { background: transparent; }

    @keyframes slideUp {
        0% { transform: translateY(15px); opacity: 0; }
        100% { transform: translateY(0); opacity: 1; }
    }
</style>
